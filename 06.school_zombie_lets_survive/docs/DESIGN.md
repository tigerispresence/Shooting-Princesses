# 학교 좀비, 살아남자! — 플레이 스펙 v0.7

`docs/STORY.md`의 승인된 스토리를 따른다. 이 문서는 **어떻게 노는가**만 정한다.
코드는 `game-builder`가 쓴다. 여기 적힌 숫자는 전부 시작값이며 플레이테스트 후 조정 대상이다.

대상: 10살 여자아이 둘. 튜토리얼 없음. 무섭지 않음. 피 없음. 물림 없음.

리뷰 문서: `docs/reviews/audience.md`, `graphic-designer.md`, `musician.md`, `story-builder.md`
빌드 결과와 편차 판정: `docs/BUILD_NOTES.md` → 이 문서 맨 끝 **§11 빌더 편차 판정**
플레이 후 판정: `docs/reviews/audience-v2.md`, `playtester.md` → **§12 audience v2 판정**
v2 범위: `docs/IDEAS_V2.md`, `docs/reviews/audience-v3-ideas.md` → **§13 v2 범위**
인게임 대사 전문은 `docs/reviews/story-builder.md` §2를 **정본**으로 삼는다. 여기서 중복하지 않는다.

---

## 변경 이력 v0.1 → v0.2

| # | 바뀐 것 | 제안한 사람 |
|---|---|---|
| 1 | **친구 구출 감지 페널티 +20% 완전 삭제.** 친구는 순수 이득(미끼 + 점수)이 됐고, 비용은 "다시 가서 깨우는 시간"뿐 | audience, story-builder |
| 2 | **스테이지 1-3에 「배고픈 좀비」 1마리 추가** (순찰 52 / 추격 88). 도망만으로는 안 되게 해서 사물함을 써 볼 이유를 만듦 | audience |
| 3 | **보스 사이클 16.7초 → 12.2초**로 단축, 교장 이동 26 → 34 px/s | audience |
| 4 | **친구 4명 전원을 스테이지 4에 배치** (4-2에 2명, 4-3에 2명). 프로필은 story-builder §3 확정본 사용 | story-builder |
| 5 | **해독제 재료 개수를 8개로 확정.** `STORY.md`의 "5개"를 8개로 고쳐야 함 (코디네이터 조치 요청) | story-builder |
| 6 | **교장 반지름 22px 확정**, 보스 잡힘 거리 28px 신설 → 부록 표에 추가 | graphic-designer |
| 7 | **미니맵 132×88로 확대 + 방 블록만 표시 + 탭하면 전체 지도(일시정지)** | graphic-designer |
| 8 | **"뮤트 상태로도 전부 클리어 가능" 규칙 신설** + 모든 정보성 소리에 시각적 쌍 지정 (§9-0) | musician |
| 9 | **터치 버튼 아이콘은 이모지 금지, 캔버스로 직접 그린다** (문서의 이모지는 표기용) | graphic-designer |
| 10 | **BGM / SFX 음소거 토글 분리** (`04.escape_room` 방식) | musician |
| 11 | **「오늘의 급식」 데일리 챌린지는 v2로 보류.** v1은 급식표·별·연습 모드로 재방문 유도 | audience |
| 12 | 친구 글로우 색을 **노랑 → 하늘색 `#7FD4FF`** (노랑이 손전등·연설 범위·급식표와 과포화) | graphic-designer §5-5 |
| 13 | 어두운 방에서 **좀비 눈에 약한 발광** 기본 적용 (완전히 안 보이면 좌절) | graphic-designer §5-2 |
| 14 | HUD 재배치 — 재료 줄을 하단 중앙으로, 미니맵은 캔버스 위 오버레이로 | graphic-designer §4-5 |
| 15 | 급식 아주머니 전용 경고 — 미니맵 반경 320px + 화면 밖 빨간 화살표 + `matronClank` | musician §5-2 |
| 16 | 스테이지 4는 **자동 줌아웃**(렌더 스케일 0.8 → 15×20 타일 시야) | audience, graphic-designer §4-2 |
| 17 | "친구 좀비" 게임오버 변주를 **「짝꿍과 함께 잠듦」**으로 개명 + 트리거 명시 | story-builder §4-3 |
| 18 | 보스에 `onDefeat` 없음 확정 — `onBroadcastSuccess` 이벤트 하나만 | story-builder §4-4 |
| 19 | 엔딩에 **구출한 친구 4명이 등장해 한마디씩** | audience |
| 20 | 신규 보너스 **「들키지 않았어!」 +400** (한 스테이지에서 한 번도 `!` 안 뜨고 재료 전부 획득) | audience |

### 거절한 제안 (검토는 했음)

| 제안 | 제안자 | 거절 이유 |
|---|---|---|
| 좀비에게 이름 붙이고 모자 씌우기 | audience | "적은 한눈에 구분돼야 한다"는 §8 규칙과 정면충돌. 좀비 실루엣은 절대 건드리지 않는다. v2 연습 모드에서만 재검토 |
| 데일리 챌린지를 v1에 넣기 | audience | 스테이지 15구역 + 보스가 먼저 재밌어야 한다. 재미의 뼈대가 안 선 채 붙이는 재방문 장치는 빈 껍데기 |
| 친구 페널티를 +10%로 낮추기 | audience | 절반짜리 페널티도 "구하면 손해"로 읽힌다. 0이 아니면 의미 없다 |
| 친구 4명 중 2명 무작위 배치 | story-builder §4-1(b) | 아이가 "다온이는 왜 없어?"라고 혼란스러워한다. 4명 고정 배치가 수집 욕구에도 맞다 |
| 재료 5개로 줄이기 | story-builder §4-2 | 스테이지당 2개 리듬이 15구역 레벨 설계의 기둥이다. `STORY.md` 쪽을 8로 고친다 |
| 손전등 감지 ×1.35 완화 | audience(간접) | audience 스스로 "무전으로 알려주면 알아챌 것 같다"고 답했다. 수치 유지 + 무전 한 줄로 해결 |
| 가로 모드 지원 | 내부 openQuestion | **세로 전용 확정.** audience: "게임은 세로로 하는 게 맞아" |
| 미니맵에 급식표 표시 | (원안 v0.1) | 급식표는 현장 반짝임으로 찾게 설계됐다. 미니맵에 넣으면 탐험의 재미가 사라진다 — graphic-designer 지적 수용 |
| 스테이지 안 미니 도전("3초 안에 줍기") | audience | 타이머 압박은 이 게임의 "기다리게 해서 벌주지 않는다" 원칙과 어긋난다. 대신 「아슬아슬」·「들키지 않았어!」 보너스로 같은 욕구를 채운다 |

---

## 0. 가장 먼저 — 이동 방식 결정

### 결론: **자유 8방향 이동** (칸 단위 이동 아님)

`04.escape_room`은 15×11 / TILE 40 / `MOVE_MS = 145`의 **칸 단위 스텝 이동**을 쓴다.
이 게임은 그 방식을 **쓰지 않는다**. 격자(타일 맵 + ASCII 레벨 데이터)는 그대로 물려받되,
플레이어와 좀비는 **float 픽셀 좌표 + 원형 충돌체**로 움직인다.

이유 (빌더가 나중에 뒤집지 않도록 명시):

1. **스텝 이동은 추격에서 반응을 막는다.** `escape_room`은 한 번 스텝을 시작하면 145ms 동안
   입력이 잠긴다(`p.moveAt` 해제 전까지 `tryStep` 호출 안 됨). 퍼즐 게임에서는 문제없지만,
   좀비가 다가올 때 145ms 입력 잠금은 "못 피했다"가 아니라 "조작이 안 먹었다"가 된다.
   아이가 억울해하는 실패는 재시도 의욕을 깎는다.
2. **Among Us 느낌의 핵심은 비껴 지나가기다.** 복도에서 좀비 옆을 스치듯 통과하는 맛은
   반 칸 단위 위치가 있어야 나온다. 칸 단위면 "같은 칸이냐 아니냐"의 이진 판정뿐이다.
3. **대각선 이동이 공짜로 생긴다.** 아이들은 조이스틱을 대각선으로 민다. 8방향이 자연스럽다.
4. **감지 반경·소리 반경·연설 범위가 전부 원형(px)이다.** 칸 단위와 섞으면 판정이 어색해진다.

### 그래도 격자는 쓴다

- 맵은 ASCII 문자열 배열 (`escape_room`의 `rooms.ts` 방식). 손으로 편집 가능해야 한다.
- 충돌은 **원 vs 타일 AABB**. 축별로 분리해서 처리한다(먼저 x 이동 → 막히면 x만 되돌림 → 그다음 y).
  이래야 벽에 대각선으로 비벼도 안 끼고 벽을 따라 미끄러진다. **벽 끼임은 이 게임 최대의 적이다.**
- 코너 보정: 이동 방향으로 막혔는데 옆으로 12px 이내에 빈 공간이 있으면 자동으로 밀어준다.

### 화면·카메라 규격

| 항목 | 값 |
|---|---|
| TILE | 40px |
| 논리 뷰포트 | 480 × 640 (12 × 16 타일), 세로 3:4 |
| **스테이지 4 한정 줌아웃** | 렌더 스케일 **0.8** → 실질 32px 타일, **15 × 20 타일** 시야. 옵션이 아니라 자동 |
| 아이폰 세로 390×844 | 캔버스 390×520 (0.8125배), HUD 상단 52px, 하단 재료줄 36px, 터치 컨트롤 236px |
| 데스크톱 | 뷰포트를 화면 높이에 맞춰 확대 (최대 1.6배) |
| **화면 방향** | **세로 전용.** 가로로 돌리면 "폰을 세워 주세요" 안내만 표시하고 게임은 일시정지 |
| 플레이어 반지름 | 13px |
| 좀비 / 아주머니 / 교장 반지름 | 14 / 16 / **22** |
| 카메라 | 플레이어 중심 추적, `cam += (target - cam) * 0.15` (60fps 기준), 맵 경계에 clamp |
| 카메라 데드존 | 없음 (항상 중앙) |
| 픽셀 스냅 | 그릴 때 카메라 오프셋만 `Math.round` — 타일 경계 실금 방지 |

---

## 1. coreLoop

> **졸음 좀비의 순찰 사이를 비집고 들어가 재료를 줍고, 들킬 것 같으면 사물함에 숨거나 분필을 던져
> 시선을 돌린 뒤, 다시 빠져나온다.**

한 사이클 15~30초.

---

## 2. controls

| 행동 | 데스크톱 | 모바일 |
|---|---|---|
| 이동 (8방향) | `WASD` / 방향키 (동시 입력) | 왼쪽 가상 조이스틱 — **동적 배치**(왼쪽 절반 아무 데나 눌러 생성), 반경 56px, 데드존 10px |
| 상호작용 (줍기·숨기·나오기·문 잠그기·친구 깨우기) | `Space` 또는 `E` | 오른쪽 하단 **큰 손 버튼** (지름 88px) |
| 분필 던지기 | 마우스 좌클릭(커서 방향) 또는 `Shift`(바라보는 방향) | **분필 버튼** (지름 68px) |
| 알람시계 놓기 (스테이지 3부터) | `Q` | **알람 버튼** (지름 68px) — 우상단에 개수 뱃지(지름 14px 원) |
| 손전등 (스테이지 3부터) | `F` | **손전등 버튼** (지름 60px) — 어두운 방에서만 나타남 |
| 전체 지도 | `M` | **미니맵을 탭** → 전체 지도 오버레이 + 게임 일시정지 |
| 일시정지 | `Esc` | HUD 우상단 ⏸ (44px) |
| 컷신 스킵 | 아무 키 | 화면 아무 데나 탭 |

규칙:
- **버튼 아이콘은 전부 캔버스로 직접 그린다. 이모지를 그리지 않는다.**
  (graphic-designer §4-4: 기기별 렌더링이 다르고 색을 못 바꾼다. 이 문서의 ✋✏️⏰🔦는 표기용 약자일 뿐)
  - 손: 펼친 손바닥 실루엣 / 분필: 짧은 흰 대각선 막대 + 끝에 가루 점 / 알람: 원 + 위에 종 두 개
  - 손전등: 원통 + 앞으로 퍼지는 삼각 빛 / 지도: 접힌 종이 사각 + 점선
- 상호작용은 **문맥 자동 선택**. 가장 가까운 대상 하나를 고르고 그 위에 아이콘 말풍선을 띄운다.
  우선순위: 잠든 친구 > 재료/급식표 > 문 > 사물함. 사거리 34px.
- 터치 버튼은 전부 `pointerdown`으로 처리 (`escape_room/TouchControls.tsx`와 동일 이유 —
  `click`은 한 박자 늦어 연타가 씹힌다). 버튼·조이스틱 영역 모두 `touch-none`.
- **390×844 세로에서 오른쪽 버튼 3개가 엄지 반경 안에 들어와야 한다.** 하단에서 위로 20~200px,
  오른쪽 끝에서 16~180px 안쪽. 이전 게임에서 버튼이 안 닿은 적이 있다.

---

## 3. mechanics[]

### M1. 졸음 좀비 (기본 적)

| 상태 | 행동 |
|---|---|
| 순찰 (patrol) | 지정된 웨이포인트 루프를 걷는다. 각 지점에서 0.8~1.6초 멈춰 하품 |
| 감지 (notice) | 플레이어가 감지 반경 안 + **벽에 가리지 않음**(직선 시야) → 머리 위 `?` |
| 추격 (chase) | `?` 시간이 끝나면 `!` 표시 + 추격. 속도 상승 |
| 포기 (giveup) | 시야에서 벗어나 일정 시간이 지나면 마지막으로 본 지점까지 간 뒤 하품하고 순찰 복귀 |

- **`?` 유예(0.5~0.6초)가 이 게임의 공정성이다.** 들켜도 즉사가 아니라 "어? 지금 뛰면 된다"가 있다.
  실패가 항상 내 판단 탓으로 느껴져야 한다.
- 시야 판정은 **원 + 직선 LOS**. 시야 콘(부채꼴)은 쓰지 않는다 — 10살에게 적이 바라보는 방향을
  읽으라는 건 요구가 너무 많다. 대신 좀비는 **하품 소리 + 보라 눈 + 머리 위 하품 말풍선**으로 위치를 알린다.
- 잡힘 판정: 중심 거리 **20px 미만** (반지름 합 27보다 작게 = 제대로 겹쳐야 잡힌다).

#### M1-b. 「배고픈 좀비」 — 스테이지 1-3에서만 (신규, v0.2)

- 급식실을 도는 **딱 1마리**. 순찰 **52 px/s**, 추격 **88 px/s**, 감지 **110px**, `?` 유예 0.6초.
- 플레이어(130)와의 속도 차가 1.76배 → **1.48배**로 좁혀진다.
  직선으로는 여전히 도망칠 수 있지만 **급식 테이블을 끼고 도는 순간 따라잡힌다** →
  아이가 처음으로 "아, 이건 숨어야겠다"고 생각하게 만드는 장치.
- 순찰 경로가 급식 테이블(`T`) 뒤를 지나서 **시야가 막힌 채로 갑자기 등장**한다.
  **이 경로만은 자동 생성이 아니라 손으로 찍는다** — "테이블 뒤에서 튀어나온다"가 이 좀비의 존재 이유다.
- 겉모습: 기본 좀비와 같되 **급식판을 들고 있고 뒤뚱거림이 눈에 띄게 빠르다**.
  나중에 나올 급식 아주머니의 예고편 역할 (graphic-designer §2의 "형태로 구분" 원칙 적용).
- 스테이지 1의 나머지 규칙(사물함 9개, 타이머 없음, 목숨 없음)은 그대로 — **여전히 거의 질 수 없다.**

### M2. 사물함 숨기

- 사물함 타일에 34px 안으로 붙어 상호작용 → 0.25초 문 닫는 연출 → **완전 무적, 감지 불가**.
- 나오기도 같은 버튼. 나오는 데 0.25초.
- **숨어 있는 시간 제한 없음.** (기다리게 해서 벌주지 않는다)
- 들어가는 걸 본 좀비는 사물함 앞에서 3.5초 하품하다 순찰로 돌아간다 → 숨기는 **시간 비용**만 든다.
- 재진입 쿨다운 **1.2초** (같은 사물함). 문 여닫이 연타로 화면이 깜빡이는 걸 막는 용도.
- 사물함 안에서는 **살짝 열린 틈**으로 밖이 그대로 보인다 (가장자리에 나무결 프레임).
  나갈 타이밍을 직접 재는 게 이 메커닉의 재미다.
- BGM에 로우패스 1200Hz만 살짝 걸어 "문틈으로 듣는" 느낌 (musician §1). **곡은 바꾸지 않는다** —
  1.2초마다 곡이 바뀌면 어지럽다.

### M3. 분필 던지기 (스테이지 2에서 습득)

- 속도 **240 px/s**, 최대 사거리 **200px**, 벽에 맞으면 그 자리에 떨어짐.
- 착지 시 **소리 반경 130px**: 그 안의 좀비가 착지 지점으로 이동해 **2.5초** 두리번거림.
- **쿨다운 1.2초, 개수 무한.** 소모품으로 만들면 아이가 다 써버리고 막힌다. 절대 막히면 안 된다.
- 이미 추격 중인 좀비도 분필에 반응한다(추격 해제). 이게 "역전 카드"라 던질 맛이 난다.

### M4. 알람시계 (스테이지 3부터 사용)

- 발밑에 놓음 → **1.0초 후 울림 시작 → 4.0초 지속**.
- 소리 반경 **220px**, 그 안의 좀비 전원을 시계 쪽으로 끌고 와 **울리는 내내 붙잡아 둔다**.
- 소지 최대 **3개**. 스테이지 시작 시 2개 보장, 맵에 1~2개 배치.
- 분필이 "잠깐 돌린다"면 알람시계는 "구역을 통째로 비운다".

### M5. 문 잠그기 (스테이지 2부터)

- 교실 문에 상호작용 → **0.6초 잠금 동작** → 그 문은 **8초간** 좀비가 못 지나감.
- 좀비는 잠긴 문을 두드리다 다른 길로 돌아간다.
- 한 문은 판당 3번까지. 남은 횟수가 문에 점 3개로 표시된다.

### M6. 어두운 방 + 손전등 (스테이지 3부터)

- 어두운 방은 어둠 오버레이. 기본 시야 원 **반경 150px**(부드러운 가장자리).
  **검정(`#000`)을 쓰지 않는다 — 짙은 남보라 계열만.**
- 손전등 ON → 바라보는 방향 **각도 70° / 사거리 260px** 콘. 색은 순백이 아니라 **`#FFF3D6`**
  (남보라 배경과의 대비 확보, graphic-designer §4-3).
- **트레이드오프**: 손전등을 켜면 좀비 감지 반경이 **×1.35**. "밝게 보고 싶다 vs 안 들키고 싶다".
  처음 켜는 순간 무전 한 줄로 알려준다 — `"손전등 켜면 잘 보여. 근데... 더 잘 들켜."`
- **어둠 속에서도 보이는 것** (막혀서 좌절하지 않게):
  - 재료·급식표 → **따뜻한 노란 반짝임** (손전등의 차가운 흰노랑과 색으로 구분)
  - **좀비 눈 → 반경 26px의 약한 보라 발광** (v0.2 신규, graphic-designer §5-2).
    벽 너머로는 보이지 않는다. "완전히 안 보이는 방"은 도전이 아니라 짜증이다.
- **어둠 오버레이는 플레이어가 그 방 "안"에 있을 때만 켠다.** 복도에서 들여다보면 어두운 바닥색은
  보인다. 다만 **어두운 방 안의 좀비·재료·급식표·친구는 밖에서 그리지 않는다** —
  밖에서 미리 정찰해 버리면 어둠이라는 메커닉이 통째로 무력해진다. 미니맵 점(250/320px)은 그대로 보인다.
- 3-2 첫 진입 시 무전: `"깜깜해도 괜찮아, 손전등 있잖아!"`

### M7. 친구 구출 — **페널티 없음** (v0.2 전면 개정)

- 숨어 있는 친구에게 상호작용 → 따라온다 (플레이어 뒤 32px를 부드럽게 추적, 여러 명이면 줄줄이).
- **이득 1 — 미끼**: 추격 중인 좀비는 **플레이어와 친구들 중 가장 가까운 대상**을 쫓는다.
  좀비가 친구에게 닿으면 친구가 그 자리에서 잠들고, **좀비는 2.0초간 옆에서 같이 하품하며 멈춘다.**
  친구는 방패이자 시간 벌이다.
- **이득 2 — 점수**: 1명당 **+700**, 스테이지 끝에 4명 전원이 깨어 있으면 별 3개 조건.
- **비용**: 잠든 친구를 다시 깨우러 가는 시간(상호작용 **1.0초**). 그게 전부다.
- **감지 반경 페널티는 완전히 삭제했다.** (v0.1의 +20%)
  이유: 구출이 조금이라도 손해로 읽히면 아이는 안 한다. 친구 구출은 **언제나 순이득**이어야 한다.
- 친구가 잡히는 순간 대사로 웃기고 넘어간다 — `"내가 너무 시끄러웠나 봐... 미안!"` (story-builder §4-5)
- **친구 때문에 게임오버가 되는 일은 없다.**

친구 4명 (프로필 정본: `reviews/story-builder.md` §3): 하늘 / 다온 / 준호 / 세은.
전원 스테이지 4에 고정 배치 — **4-2에 하늘·다온, 4-3에 준호·세은.**
세은(방송부)을 마지막에 구하면 스테이지 5 방송실로 자연스럽게 이어진다.

### M8. 급식실 아주머니 좀비 (스테이지 2부터)

- 국자를 들고 **정해진 경로를 빠르게** 왕복. 두리번거림 없이 쭉 간다.
- 빠르고 감지 반경도 넓지만 **경로가 완전히 고정**이라 외울 수 있다.
- 분필 반응 시간이 짧다(1.2초). 알람시계에는 정상 반응.
- **화면 밖에서 오는 걸 반드시 미리 알 수 있어야 한다** (musician §5-2 — 104 px/s면 250px를 2.4초에 주파):
  - 미니맵 표시 반경 **320px** (일반 좀비 250px보다 넓게) + **빨간 사각형**으로 표시 (일반은 보라 점)
  - 화면 밖 320px 이내면 **화면 가장자리에 빨간 화살표**
  - `matronClank` (국자 금속음) 0.3초 간격 반복

### M9. 교장 선생님 보스 (스테이지 5) — **사이클 단축** (v0.2)

| 단계 | v0.1 | **v0.2** | 내용 |
|---|---|---|---|
| 걷기 | 9.0초 | **5.5초** | 플레이어 쪽으로 **34 px/s**(v0.1 26). 닿으면 잡힘(거리 28px) |
| 예고 | 1.2초 | **1.0초** | 마이크를 든다. 발밑 노란 링 깜빡. "에—" |
| 연설 | 4.0초 | **3.5초** | 졸음 반경 0 → **200px로 0.8초에 걸쳐 확장** 후 유지. 안에 있으면 잡힘 |
| 회복 | 2.5초 | **2.2초** | 헛기침. 완전 무해, 이동 13 px/s |
| **총 사이클** | 16.7초 | **12.2초** | 안전 구간 7.7초 (63%) |

이동 속도를 26 → 34로 올린 건 걷기 구간이 짧아져 압박이 사라지는 걸 보정하기 위함이다.
그래도 플레이어(130)의 **1/3.8** — 달리기로는 절대 안 잡힌다.

핵심 규칙 3가지 (전부 화면으로 읽혀야 한다):
1. **연설 중엔 눈을 감고 있다** → 몸통 충돌이 꺼진다. **그냥 통과해서 지나갈 수 있다.**
2. **연설은 벽·기둥에 막힌다.** 기둥 뒤에 있으면 반경 안이어도 안전. 복도에 기둥 3개.
3. 안전 창 = 회복 2.2초 + 걷기 5.5초. **회복 시작 신호는 소리(`bossCough`)와 그림(§9-0) 양쪽으로 온다.**

보스전 목표: **해독제를 든 상태로** 방송실 문 앞에서 **상호작용을 총 3.0초 누적** → 방송 ON.
(해독제가 없으면 게이지 자체가 차지 않는다 — 5-1을 건너뛸 수 없게 하는 잠금)
진행 바는 **절대 줄어들지 않는다**(도망갔다 와도 이어서 채운다). 화면 하단에 **항상 크게** 표시.
일반 좀비 4마리는 교장 연설에 **자기들도 더 졸려져서 3초간 멈춘다** — 연설은 위협이자 기회다.

**보스에는 체력도 `onDefeat`도 없다.** 이벤트는 `onBroadcastSuccess` 하나뿐이고,
그때 교장이 `"어라? 다들 왜 이렇게 상쾌해 보이지?"`라고 반응한다. (story-builder §4-4 확인)

### M10. 급식표 조각 (수집)

- 스테이지당 **2개**, 총 **10개**. 스테이지마다 1개는 쉬운 자리, 1개는 막다른 길.
- 반경 90px 안에 들어오면 **살짝 반짝**한다. **미니맵에는 표시하지 않는다** — 현장에서 찾는 재미.
- 한 번 먹으면 **영구 저장**. 재시작해도 안 사라진다.

### M11. 미니맵 (v0.2 개정)

- 크기 **132 × 88px**. HUD 상단 띠가 아니라 **게임 캔버스 위 좌상단 반투명 오버레이**.
- 그리는 것 (graphic-designer §4-1 수용 — 단순화):
  - 방 = **단색 채운 사각형 블록** (벽선·문·타일 디테일 전부 생략)
  - 복도 = 가는 회색 막대
  - 남은 재료 = 노란 `!` 마커 / 출구 = 초록 삼각
  - 나 = 흰 점 + 진행 방향 짧은 선
  - 일반 좀비 = **보라 점, 250px 이내만** / 아주머니 = **빨간 사각, 320px 이내만**
  - 급식표 = **표시 안 함**
- **좀비 점은 벽을 무시하고 보인다.** 이건 버그가 아니라 의도다 — 벽에 막힌 하품 소리의 시각적 쌍(§9-0).
- **탭(또는 `M`)하면 전체 지도 오버레이 + 게임 일시정지.** 다시 탭하면 닫힘.
  132×88에 다 못 담는 정보는 여기서 크게 본다. 기다리게 하는 벌이 아니라 계획하는 도구.

---

## 4. levels[] — 5스테이지 × 3구역 = 15구역

각 구역은 **체크포인트**다. 잠들면 그 구역 시작 지점에서 재개하고, 이미 먹은 재료·급식표는 유지한다.
새 요소는 **구역당 하나만** 들어온다.
구역별 인게임 무전 대사는 `reviews/story-builder.md` §1 표가 정본.

### 스테이지 1 — 「점심시간 끝」 1층: 급식실·보건실·현관
맵 **30×20 타일 (1200×800px)**. 좀비 **3마리** (기본 2 + **배고픈 좀비 1**).
기본 좀비: 순찰 **45 px/s**, 추격 **74 px/s**, 감지 **100px**, `?` 유예 **0.6초**, 포기 **3.5초**.
배고픈 좀비: 순찰 **52**, 추격 **88**, 감지 **110**, `?` **0.6초**, 포기 **3.5초**.
플레이어 **130 px/s**. 재료 2개, 급식표 2개, 사물함 9개, par **90초**.

| 구역 | 새로 나오는 것 | 숫자 | 왜 더 어려운가 |
|---|---|---|---|
| 1-1 현관 → 복도 | 이동, 미니맵 | 좀비 1 (복도, 순찰 45 / 추격 74) | 시작. 좀비 한 마리가 복도를 느리게 지나갈 뿐 |
| 1-2 보건실 (비타민) | **재료 줍기** | 좀비 +1 (보건실, 45/74) | 좀비가 있는 방 **안**으로 처음 들어간다 |
| 1-3 급식실 (고추장) | **사물함 숨기** | **배고픈 좀비 1 (52/88)** + 시야를 막는 급식 테이블 | **처음으로 도망만으로는 안 된다.** 테이블 뒤에서 갑자기 나오고, 코너를 돌면 따라잡힌다 |

**스테이지 1은 여전히 거의 질 수 없다.** 사물함 9개, 타이머 없음, 목숨 없음.
다만 1-3에서 배고픈 좀비가 "숨는다는 선택지"를 아이 머릿속에 심는다.

#### 스테이지 1 ASCII 맵 (빌더 시작안, 30×20 — 연결성·도달성 검증 완료)

```
##############################
#,,,,,,,,,M,#M.#,,,,,,,,,,,,,#
#,TTT,,,TTT,#..#,TT,,,,,,,TT,#
#,TTT,,,TTT,#...,TT,,z,,,,,,,#
#,,,,,Z,,,,,...,,,,,,,V,,,,,,#
#,,,,,,,,,,,...#,,,,,,,,,,,,,#
#,LL,,,,,,G,#..#,,,,,,,,,,LL,#
#,,,,,,,,,,,#..#,,,,,,,,,,,,,#
#,,,,,,,,,,,#..#,,,,,,,,,,,,,#
#####..######..######..#######
#..........z.................#
#.......LL.........C.....LL..#
####..#######..#######..######
#,,,,,,,,,,,#..#,,,,,,,,,,,,,#
#,LLLL,,,,,,#..#,,,,,,,,,,,,,#
#,,,,,,,,,,,#..#,,,,,EEE,,,,,#
#,,,S,,,,,,,#..#,,,,,EEE,,,,,#
#,,,,,,,,,,,#..#,,,,,,,,,,,,,#
#,,,,,,,,,,,#..#,,,,,,,,,,,,,#
##############################
```

| 글자 | 뜻 |
|---|---|
| `#` | 벽 (통과 불가, 시야 차단) |
| `.` | 복도 바닥 (회색 리놀륨 `#B8BEC7` + 노란 중앙 점선 `#FFD34D`) |
| `,` | 방 바닥 (방마다 색 다름 — 팔레트는 graphic-designer §1) |
| `T` | 책상/급식 테이블/침대 — 통과 불가 + **시야 차단** (숨을 곳은 아님) |
| `L` | 사물함 / 신발장 — 통과 불가, **숨기 가능** (청록 하이라이트 테두리) |
| `V` | 재료: 보건실 비타민 |
| `G` | 재료: 매운 고추장 |
| `M` | 급식표 조각 |
| `C` | 분필 더미 (스테이지 1에서는 장식. 2에서 이 자리가 습득 지점) |
| `z` | 기본 좀비 순찰 시작점 |
| `Z` | **배고픈 좀비** 순찰 시작점 (v0.2 신규) |
| `S` | 플레이어 시작 (현관) |
| `E` | 계단 = 출구. 재료 2개를 다 모아야 열린다 |

읽는 법: 왼쪽 위가 급식실, 오른쪽 위가 보건실, 가운데 세로가 중앙 복도(2칸 폭),
한가운데 가로줄(행 10~11)이 1층 복도, 왼쪽 아래가 현관, 오른쪽 아래가 계단실.

**복도는 전부 2타일(80px) 폭이다.** 1타일 복도에서 좀비와 마주치면 대처가 없다 —
2타일이면 옆으로 비껴 지나갈 수 있다. 타협 불가 규칙.
행 1의 `M.` 은 **막다른 골목**이다. 굳이 올라가 본 아이에게 주는 상.
배고픈 좀비(`Z`)의 순찰 경로는 급식 테이블 `T` 블록 **뒤쪽을 지나가게** 잡는다.

---

### 스테이지 2 — 「5교시」 2층: 4학년 교실들·복도
맵 **34×22 (1360×880)**. 좀비 **5마리** (기본 4 + 아주머니 1).
기본: 순찰 **48**, 추격 **78**, 감지 **105**, `?` **0.55초**, 포기 **4.0초**.
아주머니: 순찰 **62**, 추격 **104**, 감지 **130**, `?` **0.45초**, 포기 **4.5초**, 경로 고정.
재료 2개(분필, 알람시계), 급식표 2개, 사물함 8개, 잠글 수 있는 문 3개, par **110초**.

| 구역 | 새로 나오는 것 | 숫자 | 왜 더 어려운가 |
|---|---|---|---|
| 2-1 2층 복도 | **분필 던지기** (여기서 습득) | 기본 **1**. 아주머니는 복도 오른쪽 끝에 **하품하며 서 있기만** 한다 | 긴 일직선 복도라 숨을 곳이 멀다 — 처음으로 **도망 말고 다른 수**가 필요하다 |
| 2-2 4학년 1반 | **아주머니 좀비** | 기본 **+1**, **분필을 줍는 순간 아주머니가 순찰을 시작한다**(104 px/s 추격) | 배고픈 좀비보다도 빠른 적이 복도를 쓸기 시작한다. 직선 도주가 거의 안 통한다 |
| 2-3 4학년 2반·3반 | **문 잠그기** | 기본 **+2**(총 기본 4 + 아주머니 1 = **5**), 문 3개, 알람시계 획득 | 방 두 개를 오가는 동안 아주머니가 복도를 계속 지난다 |

> 알람시계는 **2-3 마지막에 재료로 줍기만 하고, 도구로 쓰는 건 스테이지 3부터**다.
> 한 구역에 새 요소 둘을 넣지 않기 위한 의도적 분리.

---

### 스테이지 3 — 「특별실」 3층: 음악실·미술실·도서관
맵 **36×24 (1440×960)**. 좀비 **7마리** (기본 6 + 아주머니 1).
기본: 순찰 **52**, 추격 **82**, 감지 **110**, `?` **0.5초**, 포기 **4.0초**.
재료 2개(큰북, 웃긴 책), 급식표 2개, 사물함 6개, par **130초**.

| 구역 | 새로 나오는 것 | 숫자 | 왜 더 어려운가 |
|---|---|---|---|
| 3-1 음악실 ☀ **숨 고르기** | **알람시계 사용** | 기본 **2**, 밝음, 사물함 3개, 넓음. 큰북 + 알람시계 획득 | **일부러 쉽다.** 강력한 새 도구를 안전하게 시험하는 방. 스테이지 2보다 쉬움. **알람이 2마리를 한꺼번에 끌어가는 게 눈에 보여야 해서 2마리다 — 1마리면 분필과 구분이 안 된다** |
| 3-2 미술실 | **어두운 방 + 손전등** | 기본 **1**, 시야 150px, 콘 70°/260px, 감지 ×1.35, 눈 발광 26px. **웃긴 책 획득** | 어둠이라는 새 요소를 **한 마리로 연습**하는 방. 새 요소 뒤에는 반드시 연습 구역이 온다 |
| 3-3 도서관 | **여러 마리가 동시에 반응** | 기본 **+3**(총 기본 6 + 아주머니 1 = **7**), 책장 미로, 어둠, **출구가 이 방 안에 있다** | 어둠 연습이 끝난 뒤 처음으로 여러 마리를 상대한다. 한 마리를 분필로 돌리면 다른 마리가 그 소리로 온다 |

> **3-3은 뮤트 상태로도 클리어 가능해야 한다** (§9-0). 미니맵 보라 점 + 눈 발광 + 하품 말풍선이
> 소리 없이도 위치를 알려준다.

---

### 스테이지 4 — 「체육관·운동장」 넓은 야외 · **친구들의 스테이지**
맵 **44×30 (1760×1200)**, **렌더 스케일 0.8 자동 줌아웃** → 15×20 타일 시야.
좀비 **10마리** (기본 8 + 아주머니 2). 순찰 **56**, 추격 **88**, 감지 **120**, `?` **0.5초**, 포기 **4.5초**.
재료 2개(응원 나팔, 물호스), 급식표 2개, **친구 4명**, 숨을 곳 3개뿐, par **150초**.

| 구역 | 새로 나오는 것 | 숫자 | 왜 더 어려운가 |
|---|---|---|---|
| 4-1 체육관 | **넓은 공간 + 좀비 떼** | 기본 **3**, 매트·뜀틀 장애물 다수 | 벽이 적어 LOS가 길게 뚫린다 — 멀리서부터 들킨다 |
| 4-2 체육 창고·스탠드 | **친구 구출** | 기본 **1** + 아주머니 **1**. **하늘, 다온** 2명. 페널티 없음 | 나 혼자 피하는 게 아니라 뒤에 줄줄이 달고 다녀야 한다 |
| 4-3 운동장 | **숨을 곳 거의 없음** | 기본 **4** + 아주머니 **1** (총 기본 8 + 아주머니 2 = **10**), 사물함 0, **준호·세은** 2명 | 처음으로 "숨기"가 답이 아니다. **적이 가장 많은 구역이 마지막 구역이어야 한다.** 알람시계와 친구 미끼를 써야 뚫린다 |

- 4-1의 감지 120px은 스테이지 3보다 크지만 **맵이 넓어져서** 체감 압박은 비슷하게 유지된다.
- 줌아웃(0.8)은 audience의 "갑자기 튀어나오는 좀비 때문에 깜짝 놀란다(스트레스)" 지적 대응.
  옵션으로 두지 않는다 — 아이는 옵션 메뉴를 열지 않는다.
- **별 3개 조건에 "친구 4명 전원 깨어 있는 상태로 출구 도착"이 들어간다.**

---

### 스테이지 5 — 「과학실 → 방송실」 옥상층
맵 **32×26 (1280×1040)**. 좀비 **4마리 + 교장 선생님**.
기본: 순찰 **56**, 추격 **90**, 감지 **120**. par **170초**(보스 포함).

| 구역 | 새로 나오는 것 | 숫자 | 왜 더 어려운가 |
|---|---|---|---|
| 5-1 과학실 ☀ **숨 고르기** | **해독제 조합 연출** | **좀비 0마리.** 재료 8개를 비커에 넣는 **약 11초** 연출(스킵 가능) | **일부러 쉽다.** 숨 고르기는 연출 길이가 아니라 **좀비 없는 방을 자유롭게 돌아다니는 것**이 만든다(급식표 1개가 이 방에 숨어 있다) |
| 5-2 옥상 복도 | **교장 등장 · 연설 배우기** | 교장 1, 좀비 0, 기둥 3개, 복도 20타일 | 처음으로 **도망으로 못 푸는** 적. 사이클(5.5/1.0/3.5/2.2초)을 몸으로 익힌다 |
| 5-3 방송실 문 앞 | **교장 + 일반 좀비 동시** | 교장 1 + 좀비 4, 방송 게이지 3.0초 누적 | 연설을 피하면서 **한 자리에 멈춰 3초를 버는** 게 목표가 된다 |

5-1 조합 연출: 비커에 재료가 하나씩 들어갈 때마다 그 재료를 얻은 스테이지 이름이 뜬다.
8개가 다 들어가면 `ingredientComplete` + 보라 연기가 분홍으로 바뀐다.
길이는 **약 11초(재료당 1.1초 + 마무리 1.4초), 아무 데나 눌러 스킵 가능.**

**해독제를 만들지 않으면 방송 게이지가 차지 않는다.** 5-1을 건너뛰고 5-3에 도착하면
무전 한 줄로 막는다 — `"{이름}아, 해독제부터! 그거 없으면 방송해도 소용없어!"`

---

## 5. scoring

| 항목 | 점수 |
|---|---|
| 재료 1개 (총 8개) | **+500** |
| 급식표 조각 1개 (총 10개) | **+300** |
| 친구 구출 1명 (총 4명) | **+700** |
| 스테이지 클리어 | **+1000** |
| 시간 보너스 | `max(0, (par초 − 실제초)) × 10` |
| **「들키지 않았어!」** (v0.2 신규) | 한 스테이지에서 `!`가 **한 번도** 안 뜨고 재료 2개를 다 먹으면 **+400** |
| 「쌩쌩해요!」 | 한 스테이지에서 한 번도 안 잤으면 **+1500** |
| 잠든 횟수 | **감점 없음** — 숫자만 재미로 센다. 감점 텍스트 자체가 화면에 없다 |

화면 표시 (v0.2 재배치 — graphic-designer §4-5):
- **상단 띠 (52px)**: 점수(좌) / `급식표 3/10`(중) / ⏸(우). **정보 3개만.**
- **캔버스 위 좌상단 오버레이**: 미니맵 132×88.
- **하단 띠 (36px, 컨트롤 바로 위)**: 재료 아이콘 8칸 줄 + `Stage 1-2`.
  얻은 재료는 색이 켜지고, 못 얻은 건 회색 실루엣.
- **재료 획득 순간**: 정지 0.12초 + 아이콘이 하단 줄로 날아가는 궤적 + 상승하는 `+500`(노랑) +
  보건 선생님 무전 한 줄.
- **급식표**: 종이가 팔랑이며 상단으로 + `찰칵` + `급식표 4/10`이 잠깐 커졌다 작아짐.
- **아슬아슬** (추격 상태 좀비를 40px 이내로 스치고 빠져나감): `아슬아슬! +100` + 가장자리 흰 섬광 0.2초.
- **스테이지 클리어**: 별 1~3개가 하나씩 툭툭 찍히고 항목별 점수가 순서대로 쌓인다.
  ★ 클리어 / ★★ 재료+급식표 전부 / ★★★ 그 위에 한 번도 안 자기 (스테이지 4는 + 친구 4명 전원).

---

## 6. failState

**잡히면 죽지 않는다. 같이 잠든다.**

연출 (총 **3.0초**, 시작 후 **0.4초부터 아무 키·탭으로 스킵 가능**):
1. `0.0~1.0s` 주인공 큰 하품 → 눈 스르르. 주변 좀비도 따라 하품.
2. `1.0~2.0s` 하늘에서 베개 툭. 다 같이 쓰러져 `zzz` 말풍선. 화면 약간 어두워짐 + 코 고는 소리.
3. `2.0~3.0s` **딩동댕동** 종소리 → 주인공만 벌떡. `"아 깜짝이야! 얼마나 잤지?"`
4. 하단에 랜덤 한 줄 (10종, `reviews/story-builder.md` §2).

잡은 상대에 따른 변주 (렌더러에서 캐릭터 종류로 분기):
| 변주 | 트리거 |
|---|---|
| 국자로 푸딩 한 입 떠먹여 줌 → "맛있…" | 급식 아주머니에게 잡힘 |
| `"…그리고 세 번째로…"` 자막 | 교장 연설 반경 / 몸통에 잡힘 |
| **「짝꿍과 함께 잠듦」** — 친구와 껴안고 `zzz` 하트 | **친구를 1명 이상 데리고 다니는 상태에서** 일반 좀비에게 잡힘 |
| 기본 (베개) | 그 외 |

> v0.1의 "친구 좀비" 표기는 M7의 구출 대상 친구와 헷갈려서 폐기했다. (story-builder §4-3)

버튼: **「🔔 종 치고 다시!」** 하나뿐. 데스크톱은 `Enter`/`Space`로도 눌린다.

**재시작 규정 (타협 불가):**
- 버튼을 누른 순간부터 조작 가능해지기까지 **300ms 이내**. 페이지 리로드 없음, 에셋 로딩 없음,
  게임 상태 객체만 제자리에서 리셋.
- 재개 지점은 **현재 구역(체크포인트)의 시작**. 스테이지 처음이 아니다.
- **이미 먹은 재료·급식표는 그대로 유지.** 구출한 친구는 그 구역 시작 상태로 복귀(다시 구하면 됨,
  점수는 중복 지급하지 않는다).
- 좀비만 초기 순찰 위치로 돌아간다.
- 최악(스킵 안 함) 3.0초 + 300ms, 스킵하면 **0.4초 + 300ms 미만**.
- 목숨 개념 없음. 무한 재시도. 대기 시간 없음.

---

## 7. progression

**시작 화면 (첫 실행)**
- 이름 입력: 한글 최대 6자. 추천 이름 버튼 4개(`하린` `서아` `유나` `지우`).
- 외모: 머리색 4종 × 옷색 4종 = 16조합 (색 코드는 graphic-designer §1). 큰 미리보기.
- `localStorage` 저장 → 다음부터 `"{이름}아, 또 왔구나!"` + 바로 이어하기.

**저장되는 것** (`localStorage`, 키 하나에 JSON)
| 키 | 내용 |
|---|---|
| `name`, `look` | 이름, 외모 |
| `unlockedStage` | 해금된 스테이지 (1~5) |
| `stars[5]` | 스테이지별 별 0~3 |
| `best[5]` | 스테이지별 최고 점수 |
| `menuPieces` | 급식표 조각 10칸 (한 번 먹으면 영구) |
| `friendsMet` | 만난 친구 4명 (엔딩 등장 여부) |
| `sleepCount` | 총 잠든 횟수 (엔딩 개그용) |
| `unlockedHats` | 얻은 모자 |
| `bgmOn`, `sfxOn` | **음악/효과음 토글 분리 저장** |

**해금**
- 스테이지 선택 화면. 클리어하면 다음 칸이 열린다. 깬 스테이지는 언제든 재도전.
- **급식표 10개 완성** → 엔딩에 "내가 고른 메뉴"가 급식표에 오름 + 🍓 **딸기 푸딩 모자**.
- 별 누적 해금: 5별 `리본` / 10별 `왕관` / 15별 `급식 모자`.
  모자는 전부 **장식 전용** — 성능 영향 없음. 실력 없는 아이가 밀리면 안 된다.
- **엔딩에 구출한 친구 4명이 등장해 한마디씩 한다** (v0.2, audience 요청).
  못 구한 친구는 그 자리가 비고, 다시 플레이할 이유가 된다.

**내일 또 켤 이유 (v1)**
- 급식표 칸이 비어 있는 게 수집 화면에 회색 실루엣으로 보인다.
- 스테이지별 최고 점수 + 별 3개 도전 (특히 스테이지 4의 친구 4명 전원).
- 엔딩 후 해금: **「연습 모드」** — 좀비 수를 0~최대로 직접 조절해 맵을 마음껏 돌아다니기.

**v2 예정 — 「오늘의 급식」 데일리 챌린지**
audience가 가장 강하게 원한 기능(`"이건 무조건 해야 해!!"`)이지만 **v1에는 넣지 않는다.**
15구역 + 보스가 먼저 재밌어야 하고, 재미의 뼈대가 안 선 상태에서 붙이는 재방문 장치는 빈 껍데기다.
v2 설계는 지금 적어 둔다(잊지 않기 위해):
- 날짜를 시드로 매일 1개 챌린지. 예: `"분필 없이 2스테이지 클리어"`, `"30초 안에 1-3 통과"`,
  `"아무도 안 깨우고 4스테이지 클리어"`.
- 성공하면 급식표에 **그날의 특별 메뉴 칸**이 하루만 켜지고, 주간 7칸을 다 채우면 모자 하나.
- 실패해도 잃는 건 없다. 다음 날 새 챌린지.

---

## 8. graphic-designer 에게 — 반드시 시각적으로 구분되어야 하는 것

색·실루엣 상세는 `reviews/graphic-designer.md` §1~3이 정본. 여기는 **구분 요구사항**만 남긴다.

**절대 헷갈리면 안 되는 짝:**

1. **졸음 좀비 vs 구출할 친구** — 좀비: 보라 눈 + 반쯤 감긴 눈꺼풀 + **축 늘어진 팔** + 하품 말풍선.
   친구: 또렷한 흰 눈 + **하늘색 `#7FD4FF` 테두리 글로우(고리형, 펄스)** + 손 흔들기.
   **v0.2 변경: 친구 글로우를 노랑 → 하늘색으로.** 노랑은 이미 손전등·연설 범위·급식표·`+점수`에
   쓰여 과포화다. (graphic-designer §5-5 수용)
2. **사물함(숨기 가능) vs 장식 가구** — 숨을 수 있는 것에만 **연한 청록 하이라이트 테두리**,
   가까이 가면 `↓` 아이콘.
3. **재료 vs 급식표 조각** — 재료는 크고 빛나는 실물 아이콘(**타일의 60% 이하** 크기,
   글로우는 아이콘 밖으로 삐져나오게). 급식표는 10개 전부 **똑같은 연노랑 접힌 종이** + 반짝임 파티클.
4. **기본 좀비 vs 배고픈 좀비 vs 급식 아주머니** — 세 단계가 속도 순으로 읽혀야 한다.
   기본: 빈손. **배고픈 좀비: 급식판을 들고 뒤뚱임이 빠름.** 아주머니: 한 덩치 크고(r16)
   빨간 머릿수건 + 흰 앞치마 + 국자 실루엣. **흑백으로 봐도 구분되어야 한다.**
5. **교장 연설 범위 vs 손전등 불빛** — 연설은 **바닥에 깔리는 노란 반투명 원 + 테두리 맥동**,
   손전등은 **위로 뜨는 `#FFF3D6` 부채꼴**. 둘 다 같은 노랑을 쓰지 말 것.
6. **분필 소리 반경 vs 알람시계 소리 반경** — 분필: **얇은 흰 원 1회 퍼짐**.
   알람: **두꺼운 주황 원이 4초간 반복 + 남은 시간이 줄어드는 링**. 지속 여부가 한눈에.
7. **잠긴 문 vs 안 잠긴 문** — 잠기면 자물쇠 아이콘 + 문틀이 파랗게. 남은 횟수 점 3개.
8. **복도 바닥 vs 방 바닥** — 복도는 전 스테이지 공통 회색 리놀륨 + 노란 중앙 점선.
   길찾기의 기준선이다. 방은 방마다 다른 색.
9. **`?` 와 `!`** — 이 게임의 공정성 그 자체. 상세 스펙은 graphic-designer §3 표를 그대로 채택.
   흰/작게/떠오름 vs 빨강/2배/튕김 + 화면 진동 ±2px.
10. **교장의 4단계 상태** (v0.2 신규 — §9-0 뮤트 대응) — 걷기 / 예고 / 연설 / **회복**이
    소리 없이도 구분돼야 한다. 특히 회복 = "지금 가라" 신호다.
11. **어두운 방의 좀비 눈** — 반경 26px 약한 보라 발광. 벽 너머로는 안 보임.
12. **재료 반짝임(따뜻한 노랑) vs 손전등 콘(차가운 흰노랑)** — 손전등 안에서 반짝임이 묻히지 않게.
    (graphic-designer §5-6 수용)

**전체 톤**: 파스텔, 둥근 모서리, 굵은 외곽선 2px. 그림자는 발밑 타원 하나.
어두운 방도 검정이 아니라 **짙은 남보라**. 캐릭터 디테일은 **3개 이하**(몸색 / 눈 / 머리 위 아이콘).
무서운 건 하나도 없다.

---

## 9. musician 에게 — 사운드

작곡·합성 레시피는 `reviews/musician.md`가 정본. 여기는 **설계 쪽 결정과 제약**만 적는다.

### 9-0. 최우선 규칙 — 뮤트 상태로도 전부 클리어 가능해야 한다 (v0.2 신규)

> **소리는 예고, 확정 정보는 항상 시각.**
> 스테이지 1~5 어느 구역도 청각 정보를 **필수**로 요구하지 않는다. 3-3 도서관도 예외가 아니다.
> (musician §5-4 질문에 대한 답: **필수 청각 구간은 만들지 않는다.**)

게임플레이 정보를 나르는 모든 소리에는 **시각적 쌍**이 있다:

| 소리 | 나르는 정보 | 시각적 쌍 |
|---|---|---|
| `yawn` (팬/거리 조절) | 좀비의 대략적 위치 | 미니맵 보라 점(250px, **벽 무시**) + 화면 안 실루엣 + 머리 위 하품 말풍선 |
| `yawn` 벽 너머 먹먹 버전 | 벽 뒤에 누가 있다 | 미니맵 점이 벽을 무시하고 보이는 것 자체가 이 소리의 시각 번역이다 |
| `matronClank` | 아주머니가 빠르게 접근 중 | 미니맵 **빨간 사각(320px)** + 화면 밖이면 **가장자리 빨간 화살표** |
| `notice` | 유예 0.6초 시작 | 흰 `?` 말풍선 + 좀비 눈 커짐 |
| `alert` | 추격 시작 | 빨간 `!` (2배 크기, 튕김) + 화면 진동 ±2px |
| `bossCough` | **회복 구간 시작 = 지금 가라** | 교장이 **허리를 펴는 동작** + 발밑 노란 링이 **초록 링**으로 교체 + 머리 위 초록 점 + 방송 게이지 바 테두리가 초록으로 빛남 |
| `bossSpeechLoop` | 연설 중 = 통과 가능 | 노란 반투명 원 + 교장 눈이 `>_<`로 감김 + 화면 하단 연설 자막 |
| `alarm` | 4초간 좀비를 붙잡는 중 | 두꺼운 주황 원 4회 반복 + 남은 시간 링이 줄어듦 |
| `chalk` 착지 | 여기로 시선이 쏠린다 | 얇은 흰 원 1회 + 착지점 작은 흰 `!` |
| `broadcastTick` | 방송 게이지 진행률 | **화면 하단 큰 바 — 5-3 내내 항상 표시** (musician §5-3 확인 요청에 대한 답: 그렇다) |
| `bell` | 재시작 가능 | 「종 치고 다시!」 버튼이 통통 튀며 등장 |
| `stageStart` | 구역 시작/재시작 완료 | 구역 이름 카드 0.8초 |

### 9-1. 음소거 토글 분리 (v0.2, musician §5-5 수용)

- `04.escape_room`처럼 **bgmBus / sfxBus를 나눈다.**
- 일시정지 화면에 토글 **2개**: `음악` / `효과음`. 기본 둘 다 켬. `localStorage` 저장.
- HUD 우상단 ⏸ 옆에 **빠른 음소거 1개**(둘 다 끔/켬)도 둔다 — 급할 때 한 번에.
- 이 게임은 소리가 정보를 나르므로 **"음악만 끄고 효과음은 켜기"가 기본 권장 조합**이다.

### 9-2. 보스 사이클 단축에 따른 재작업 (v0.2)

보스 타이밍이 바뀌었으니 다음 소리의 길이를 맞춰야 한다:
- `bossSpeech` (예고) 1200ms → **1000ms**
- `bossSpeechLoop` (연설) 4000ms → **3500ms** (음절 7~9개)
- `bossCough` 300ms 유지, 회복 구간 2.2초 시작점에 정확히 동기
- 보스 행진곡 루프도 12.2초 사이클에 맞춰 자연스럽게 되감기게

### 9-3. 그 외 수용한 제안

- **졸음 모티프 솔-미-도-라** 재사용 설계(musician §4) — 전면 채택. 아이가 "이 소리 = 졸림"을
  학습하는 게 이 게임의 청각 정체성이다.
- 사물함 안 BGM 로우패스 1200Hz, **곡은 안 바꿈** — 채택.
- `matronClank` 신규 SFX — 채택 (§M8).
- `yawn` 벽 감쇠(로우패스 1500Hz + ×0.5) — 채택.
- `nearMiss`가 추격 중에 튀는 소리로 들릴 위험 — **playtester 확인 항목으로 등록** (§10-5).

### 9-4. iOS 주의

`AudioContext`는 정지 상태로 시작한다. 타이틀 화면 **「시작」 버튼의 실제 `touchend`/`click`
핸들러 안에서** `resume()`을 불러야 한다. 이 프로젝트에서 두 번 깨진 적 있다.

---

## 10. openQuestions — 빌드 직후 playtester가 확인할 것

v0.1에서 답이 나온 항목(스테이지 1 난이도, `?` 유예, 분필 무한, 친구 페널티, 보스 사이클,
가로 모드, 데일리 챌린지, 급식표 개수)은 **§변경 이력에서 결정 완료**. 남은 것만 적는다.

1. **배고픈 좀비(52/88)가 스테이지 1을 "거의 질 수 없음"에서 벗어나게 했는가?**
   의도는 "숨어볼 이유를 만들되 여전히 안 진다"이다. 1-3에서 아이가 **두 번 이상** 잠들면 과하다
   → 추격 88 → 84로 내린다.
2. **친구 미끼 메커닉이 "친구를 일부러 희생시키는" 플레이로 변하지 않는가?**
   페널티를 없앤 대신 친구가 방패가 됐다. 아이가 친구를 고의로 좀비 앞에 밀어 넣으며 웃는다면
   그건 귀여운 창발이지만, "안 깨우고 버리는" 게 최적해가 되면 정서적으로 안 좋다.
   → 그렇게 나오면 **스테이지 종료 시 친구 전원 각성 보너스(+1000)**를 추가한다.
3. **보스 12.2초 사이클이 이제 너무 촉박하지 않은가?** 5.5초 걷기 + 34 px/s로 압박을 보정했는데,
   방송 게이지 3.0초를 세 번 나눠 채우기에 안전 창 7.7초가 빠듯할 수 있다.
   → 빠듯하면 연설 3.5 → 3.2초.
4. **재료 8개 HUD 줄이 390px에서 읽히는가?** 8칸 × 약 28px = 224px. 이론상 들어가지만
   실제로 "뭘 더 모아야 하는지" 한눈에 읽히는지 확인 필요. 안 읽히면 4+4 두 줄.
5. **`nearMiss` 소리·흰 섬광이 추격 중에 놀라게 하지 않는가?** (musician §5-6)
6. **스테이지 4 줌아웃(0.8)에서 캐릭터가 너무 작지 않은가?** 폰 실측 지름이 약 17px까지 내려간다.
   graphic-designer의 "디테일 3개 이하" 규칙이 여기서도 버티는지 확인.
7. **전체 지도(미니맵 탭 → 일시정지)를 아이가 발견하는가?** 발견 못 하면 1-1에서 무전으로 한 번
   알려준다: `"지도를 눌러 봐. 어디가 어딘지 다 보여!"`
8. **뮤트 상태 클리어 검증** — playtester는 **스테이지 3-3과 5-3을 소리 완전히 끈 채로** 한 번씩
   클리어해 봐야 한다. 이게 §9-0 규칙의 실제 검증이다.

---

## 부록 A — 빌더용 숫자 요약표

| 상수 | 값 |
|---|---|
| `TILE` | 40 |
| `VIEW_W` × `VIEW_H` | 480 × 640 |
| `RENDER_SCALE` | 1.0 (스테이지 1~3, 5) / **0.8 (스테이지 4)** |
| `PLAYER_SPEED` | 130 px/s |
| `PLAYER_R` / `ZOMBIE_R` / `MATRON_R` / **`BOSS_R`** | 13 / 14 / 16 / **22** |
| `CATCH_DIST` / **`BOSS_CATCH_DIST`** | 20 px / **28 px** |
| `INTERACT_DIST` | 34 px |
| `NOTICE_DELAY` | 0.60 → 0.55 → 0.50 → 0.50 → 0.50 s (스테이지 1~5) |
| `GIVEUP_MS` | 3500 → 4000 → 4000 → 4500 → 4500 |
| `PATROL_SPEED` | 45 → 48 → 52 → 56 → 56 |
| `CHASE_SPEED` | 74 → 78 → 82 → 88 → 90 |
| `NOTICE_R` | 100 → 105 → 110 → 120 → 120 |
| `ZOMBIE_COUNT` (**정본**, 아주머니 포함) | 3 → 5 → 7 → 10 → 4(+보스) |
| └ 구역별 내역 (§4 표와 합이 일치해야 한다) | **S1** 1/1/1(배고픈) · **S2** 1/1+A/2 · **S3** 2/1/3+A · **S4** 3/1+A/4+A · **S5** 4+보스 |
| **`HUNGRY_*`** (배고픈 좀비, 1-3 전용) | 순찰 **52** / 추격 **88** / 감지 **110** / `?` 0.6s |
| `MATRON_*` | 순찰 62 / 추격 104 / 감지 130 / `?` 0.45s |
| `CHALK_SPEED` / `RANGE` / `NOISE_R` / `DISTRACT_MS` / `CD` | 240 / 200 / 130 / 2500 / 1200 |
| `ALARM_DELAY` / `RING_MS` / `NOISE_R` / `MAX` | 1000 / 4000 / 220 / 3개 |
| `LOCK_MS` / `LOCK_CAST` / `LOCK_USES` | 8000 / 600 / 3 |
| `DARK_VIEW_R` / `TORCH_ANGLE` / `TORCH_RANGE` / `TORCH_NOTICE_MULT` | 150 / 70° / 260 / 1.35 |
| **`DARK_EYE_GLOW_R`** | **26** (어두운 방 좀비 눈 발광, 벽 너머 불가) |
| `HIDE_ENTER_MS` / `HIDE_RECD_MS` / `ZOMBIE_WAIT_MS` | 250 / 1200 / 3500 |
| `FRIEND_FOLLOW_DIST` / **`FRIEND_NOTICE_MULT`** / `WAKE_MS` / **`FRIEND_STALL_MS`** | 32 / **1.00 (페널티 없음)** / 1000 / **2000** |
| **`FRIEND_COUNT`** | **4** (4-2에 2명, 4-3에 2명) |
| **`INGREDIENT_COUNT`** | **8** (스테이지 1~4 각 2개) |
| `MENU_PIECE_COUNT` | 10 (스테이지당 2개) |
| 보스 `WALK_MS`/`WINDUP_MS`/`SPEECH_MS`/`RECOVER_MS` | **5500 / 1000 / 3500 / 2200** (총 12200) |
| 보스 `WALK_SPEED` / `RECOVER_SPEED` / `SLEEP_R` / `EXPAND_MS` | **34** / 13 / 200 / **800** |
| `BROADCAST_HOLD_MS` | 3000 (누적, 감소 없음) |
| `MINIMAP_W` × `MINIMAP_H` | **132 × 88** |
| `MINIMAP_ZOMBIE_R` / **`MINIMAP_MATRON_R`** | 250 / **320** |
| `GAMEOVER_MS` / `SKIP_AFTER_MS` / `RESTART_BUDGET_MS` | 3000 / 400 / 300 |
| par (초) | 90 / 110 / 130 / 150 / 170 |
| 맵 크기 (타일) | 30×20 / 34×22 / 36×24 / 44×30 / 32×26 |
| HUD 상단 / 하단 재료줄 (390px 기준) | 52px / 36px |

---

## 부록 B — 코드 배치 (`CLAUDE.md` 관례 준수)

```
src/app/game/
  types.ts        Phase, Zombie, Friend, Item, MapDef, HudState …
  constants.ts    부록 A 전부 + 스테이지 메타 + 팔레트
  maps.ts         스테이지별 ASCII 맵 + 웨이포인트 + 아이템/친구 좌표
  engine.ts       이동/충돌/AI/감지/추격/친구/보스 상태기계
  renderer.ts     배경, 어둠, 시야, 미니맵, HUD, 파티클
  sprites.ts      캐릭터·사물·버튼 아이콘 캔버스 드로잉 (이모지 금지)
  audio.ts        Web Audio 엔진 + unlock + bgmBus/sfxBus 분리
  music.ts        스테이지별 시퀀서
  text.ts         인게임 대사 (reviews/story-builder.md §2를 옮긴 것)
  storage.ts      localStorage (이름·외모·별·급식표·친구·사운드 설정)
  GameCanvas.tsx  게임 루프 + 입력 (상태는 전부 ref)
  TouchControls.tsx  조이스틱 + 버튼 3~4개
  NameEntry.tsx   이름 입력 + 외모 선택
  StageSelect.tsx 스테이지 선택 + 별 + 급식표 수집 화면
  PauseMenu.tsx   일시정지 + 음악/효과음 토글 + 전체 지도
```

게임 루프 안에서는 **React state를 절대 쓰지 않는다**. HUD로 넘기는 값은
`escape_room`의 `toHud()`처럼 초 단위로 잘라서 넘겨 리렌더를 줄인다.

---

## 11. 빌더 편차 판정 (v0.3)

`docs/BUILD_NOTES.md` §2의 편차 10건에 대한 판정이다.
**수락**이면 이 문서(§0~§10, 부록 A)를 코드에 맞춰 이미 고쳤다 — 스펙과 코드가 지금 일치한다.
**반려**면 빌더가 코드를 고쳐야 한다. 반려 항목은 아래 §11-B에 모아 뒀다.

먼저 칭찬해 둘 것: 재시작 **17ms**(예산 300ms), 벽 끼임 0회, 이모지 0개, 390×844 버튼 4개 전부
엄지 반경 안. 이 네 가지가 이 게임에서 가장 깨지기 쉬운 곳인데 전부 지켜졌다.

### 11-A. 수락한 편차 (스펙을 코드에 맞춰 수정함)

| # | 편차 | 판정 | 이유 |
|---|---|---|---|
| 1 | 좀비를 구역별 스폰이 아니라 스테이지 시작에 전부 배치 | **수락** | 좀비가 자기 방을 안 벗어나므로 만나는 순서는 설계대로다. 오히려 갑자기 튀어나오는 스폰보다 낫다 — audience가 "갑자기 튀어나오는 좀비는 스트레스"라고 했다. 단, 스테이지 2 아주머니만 예외(§11-B-1) |
| 2 | 일반 좀비 순찰 경로를 씨앗 기반 자동 생성 | **수락** | 씨앗이 고정이라 매번 같은 길이면 "외울 수 있다"는 성질이 유지된다. 손으로 15구역 × 여러 마리를 찍는 건 유지보수 비용만 크다. 단, 1-3 배고픈 좀비만 예외(§11-B-2) |
| 3 | 5-1 조합 연출 30초 → 약 11초, 스킵 가능 | **수락** | 빌더 판단이 맞다. "10살이 30초짜리 컷신을 두 번 보지는 않는다"는 내 「기다리게 해서 벌주지 않는다」 원칙 그대로다. **숨 고르기의 정체는 연출 길이가 아니라 좀비 0마리인 방을 자유롭게 돌아다니는 것**이었다 — 과학실에 급식표가 하나 숨어 있으니 그 기능은 이미 충족된다. §4 스테이지 5 표를 11초로 고쳤다 |
| 5 | 스테이지 5에도 급식표 2개 배치 | **수락** | 원래 스펙이 "스테이지당 2개 × 5 = 10개"였는데 5스테이지 배치만 안 적었다. 내 누락이다 |
| 6 | 방송 게이지가 해독제를 만든 뒤에만 참 | **수락** | 스펙 구멍을 정확히 메웠다. 잠금이 없으면 5-1을 건너뛰고 보스로 직행할 수 있었다. §M9와 §4에 잠금 조건과 무전 대사를 명문화했다 |
| 8 | 어둠 오버레이를 "플레이어가 방 안에 있을 때만" 켬 | **조건부 수락** | 복도가 갑자기 어두워지는 것보다 낫다. 다만 **밖에서 방 안의 좀비·재료·급식표·친구를 그리면 안 된다** — 밖에서 정찰되면 어둠 메커닉이 무력해진다. §M6에 명문화했다. 이미 그렇게 되어 있으면 변경 없음, 아니면 §11-B-5 |
| 9 | 하단 띠 40px, 지도 버튼 제거 | **수락** | 애초에 §2 컨트롤 표에 하단 지도 버튼은 없었다(미니맵 탭 / `M` 두 가지뿐). 손 버튼과 겹치는 버튼은 없는 게 맞다. 36 → 40px은 부록 A에 반영 |
| — | 맵 2~5를 빌더가 새로 그림 | **대체로 수락** | 2타일 복도 규칙·도달성 전부 지켰고 방 배치도 §4 의도에 맞다. **다만 스테이지 3·4는 구역이 가르치려는 것을 실제로 가르치지 못한다**(§11-B-3, §11-B-4) |

### 11-B. 반려 — 빌더가 고쳐야 할 것

우선순위 순이다. **1·3번이 커브를 실제로 깨는 것이고, 나머지는 마감이다.**

---

**B-1. 스테이지 2 아주머니가 2-1부터 복도 전체를 쓸고 다닌다 — 「구역당 새 요소 하나」 위반**

- 현상: `matronPaths[0]`이 `x 2.5 ↔ 31.5`, 즉 복도 **전 구간**이다. 그런데 2-1의 `zone`이
  `{x:1,y:9,w:32,h:2}`(같은 복도)이고 `spawn`이 `(3.5, 10.5)`다.
  → **분필을 배우는 구역에서 아주머니를 동시에 만난다.** 2-2의 무전
  `"국자 아주머니는 빨라!"`는 이미 만난 뒤에 나온다.
- 고칠 것: 아주머니는 **복도 오른쪽 끝(x≈31)에 서서 하품만 하고 있다가,
  플레이어가 분필(`C`)을 줍는 순간 순찰을 시작한다.**
  경로 자체는 지금 것을 그대로 쓰면 된다 — 시작 조건만 붙이면 된다.
- 왜 이 방식인가: 맵도 구역도 안 건드린다. 그리고 "분필을 주웠더니 저 멀리서 국자 소리가
  나기 시작한다"는 **기억에 남는 한 장면**이 공짜로 생긴다.

---

**B-2. 1-3 배고픈 좀비의 순찰 경로가 자동 생성이다**

- 이 좀비의 존재 이유는 속도가 아니라 **"급식 테이블 뒤에서 시야가 막힌 채 갑자기 나온다"**이다
  (§M1-b). BFS로 방 안 먼 지점 3곳을 이으면 그게 보장되지 않는다.
- 고칠 것: `Z`의 웨이포인트만 손으로 찍는다. 급식실 테이블 블록
  (`MAP1` 행 2~3의 `TTT` 두 덩어리) **뒤쪽을 지나가는 8자 경로**로.
- 나머지 일반 좀비는 자동 생성 그대로 둔다.

---

**B-3. 스테이지 3 — 손전등을 배우는 3-2 미술실을 통째로 건너뛸 수 있다 (가장 중요)**

- 현상: 재료가 `O` 큰북(음악실) + `J` 웃긴 책(**도서관**)이고, 출구 `EE`는 **복도**(행 10~11, 열 33~34)에 있다.
  → 미술실에는 급식표 하나뿐이라 **안 들어가도 클리어된다.**
  그러면 아이는 어둠을 연습하지 못한 채 3-3 도서관(어둠 + 좀비 3 + 책장 미로)에 들어간다.
  이건 내 커브 규칙 「새 요소 뒤에는 반드시 연습 구역을 한 번 준다」의 정면 위반이다.
- 고칠 것 (`MAP3`, 세 군데):
  1. **`J`(웃긴 책)를 도서관 → 미술실로 옮긴다.** (예: 행 7, 열 30 근처의 `,`)
  2. **출구 `EE`를 복도 → 도서관 안쪽으로 옮긴다.** (예: 행 20~21, 열 32~33)
     복도의 기존 `EE` 자리는 그냥 복도 바닥 `.`으로.
  3. 결과 동선: 음악실(큰북·알람) → 미술실(웃긴 책·손전등 연습) → 도서관(출구). **세 방 전부 필수, 순서도 고정.**
- 같이 고칠 것 — **좀비 배분 (총 6 + 아주머니 1 = 7은 그대로)**:
  - **음악실 1 → 2마리.** 알람시계를 처음 쓰는 방인데 1마리면 "싹 다 몰려온다"가 안 보인다.
    분필과 구분이 안 된다. 이 구역의 존재 이유가 사라진다.
  - **미술실 2 → 1마리.** 어둠을 처음 만나는 연습 구역이다. 한 마리로 충분하다.
  - 도서관 3마리·아주머니 복도 순찰은 그대로.
  - 합계 2 + 1 + 3 + 아주머니 1 = **7** ✓ (부록 A와 일치)
- 참고: 이 배분은 BUILD_NOTES §2-2의 "3-1이 너무 심심하면 2로 올릴 것"에 대한 답이기도 하다 —
  playtester를 기다리지 말고 **지금 2로 올린다.**

---

**B-4. 스테이지 4 — 마지막 구역이 첫 구역보다 적이 성기다**

- 현상: 체육관(4-1) 기본 4마리 / 운동장(4-3) 기본 3마리 + 아주머니 1.
  면적은 체육관 23×13 = 299칸, 운동장 42×14 = 588칸.
  → **클라이맥스의 밀도가 도입부의 절반 이하다.** 4-3은 "숨을 곳이 없어서 도구를 써야 뚫리는 곳"인데
  지금 숫자로는 그냥 걸어서 가로지른다.
- 고칠 것: **체육관 `z` 한 마리를 운동장으로 옮긴다.** 총합 10은 그대로.
  - 4-1 체육관 **3** / 4-2 창고 **1 + 아주머니 1** / 4-3 운동장 **4 + 아주머니 1**
  - `MAP4`에서 `(13,19)`의 `z`를 지우고 운동장 가운데(예: 행 22, 열 24 근처)에 하나 추가.
- 더 조일지는 playtester 결과를 보고 정한다. 지금은 **순서만 바로잡는다.**

---

**B-5. 어두운 방 밖에서 방 안 엔티티가 보이면 안 된다**

- §11-A-8의 조건. 복도에서 도서관을 들여다봤을 때 **어두운 바닥색은 보여도 되지만,
  그 안의 좀비·재료·급식표·친구는 그리지 않는다.**
- 이미 그렇게 구현돼 있으면 아무것도 안 해도 된다. 확인만 부탁한다.

---

**B-6. 잠긴 문 두드리기(`knock`)가 안 붙어 있다**

- §M5에 "좀비는 잠긴 문을 두드리다 다른 길로 돌아간다"고 적혀 있다.
  지금은 말없이 돌아가서 **문을 잠근 게 효과가 있었는지 알 수 없다.**
- 고칠 것: 잠긴 문에 막힌 좀비가 경로를 다시 잡을 때 문 앞에서 **0.6초 멈추고 `knock` 3회** +
  문틀이 세 번 흔들린다. `audio.ts`에 `knock`은 이미 있다.
- 이건 정보성 소리가 아니라 **보상 피드백**이다. 뮤트 대응(§9-0)은 문틀 흔들림이 담당한다.

### 11-C. 표 불일치 정리 (§4 구역 표 ↔ 부록 A)

빌더 지적이 맞다. v0.2의 구역 표를 그대로 더하면 스테이지 3이 부록 A보다 1마리 많았다
("3-1 좀비 2 / 3-2 +2 / 3-3 +3" = 기본 7인데 부록 A는 기본 6 + 아주머니 1 = 7).
스테이지 2의 "+1 아주머니"도 "기본 1마리 추가 **및** 아주머니"인지 "추가되는 1마리가 곧 아주머니"인지
읽는 사람마다 달라지는 표기였다.

**정리 규칙 (이제부터 고정):**
- **부록 A의 `ZOMBIE_COUNT`가 정본이고, 그 숫자는 아주머니를 포함한 총합이다.**
- §4 구역 표는 "기본 N" / "아주머니 1"을 **따로** 적는다. "+1 아주머니" 같은 표기는 쓰지 않는다.
- 부록 A에 **구역별 내역 한 줄**을 추가했다. 두 표의 합이 다르면 그건 스펙 버그다.

| 스테이지 | 구역별 (기본, A=아주머니) | 총합 = 부록 A |
|---|---|---|
| 1 | 1-1 **1** / 1-2 **1** / 1-3 **1**(배고픈) | **3** ✓ |
| 2 | 2-1 **1** / 2-2 **1 + A** / 2-3 **2** | **5** ✓ |
| 3 | 3-1 **2** / 3-2 **1** / 3-3 **3 + A**(A는 복도 순찰) | **7** ✓ |
| 4 | 4-1 **3** / 4-2 **1 + A** / 4-3 **4 + A** | **10** ✓ |
| 5 | 5-3 **4** + 교장 | **4 + 보스** ✓ |

### 11-D. 빌더가 넘긴 질문에 대한 답

| 질문 (BUILD_NOTES) | 답 |
|---|---|
| "3-1 음악실이 심심하면 2마리로 올릴 것" | **지금 올린다.** playtester를 기다릴 문제가 아니다 (§11-B-3) |
| "30초를 고수하면 `updateRitual`의 1100을 3500으로" | **고수하지 않는다.** 11초 유지 (§11-A-3) |
| "어둠을 '화면에 보이면 켜기'로 바꿔야 하나" | **바꾸지 않는다.** 대신 밖에서 방 안 엔티티를 안 그리면 된다 (§11-B-5) |
| "하단 재료 8칸이 뭉개지면 4+4 두 줄" | playtester 결과를 보고 정한다. openQuestion #4 그대로 유지 |
| "기본 좀비와 배고픈 좀비 실루엣 차이가 급식판뿐이라 약하다" | graphic-designer 소관. **급식판 + 몸을 앞으로 기울인 자세**까지 두 가지로 가면 작게 봐도 읽힌다 |

### 11-E. 이번 판정으로 바뀐 openQuestions

- **`BUILD_NOTES` playtester 목록의 #2 「3-1 음악실 1마리가 심심한가」는 확인 항목에서 뺀다.**
  2마리로 확정했으므로 더 물어볼 것이 없다 (§11-B-3).
- **신규**: B-3의 동선 변경(음악실 → 미술실 → 도서관 일방향) 뒤에도 스테이지 3이 par 130초 안에
  들어오는가. 세 방을 전부 도는 게 강제되므로 par를 **150초**로 올려야 할 수 있다.
  playtester가 3스테이지 클리어 실측 시간을 보고해 주면 그때 정한다.
- **신규**: B-1의 "분필을 주우면 아주머니가 움직이기 시작한다"를 아이가 인과로 읽는가,
  아니면 그냥 우연으로 느끼는가.

---

## 12. audience v2 판정 (v0.4)

빌드된 게임을 10살이 직접 해 보고 **9/10**을 줬다 (`docs/reviews/audience-v2.md`).
"10이 아닌 이유는 좀비가 좀 더 다양하면 더 완벽할 것 같아서"가 유일한 감점 사유다.
그 1점을 가져오는 게 이번 판정의 목표다.

먼저 확인해 둘 것 — v0.2에서 걱정했던 네 가지가 전부 해소됐다고 본인이 말했다:
친구 페널티 삭제 ✓ / 스테이지 1 배고픈 좀비 ✓ / 보스 12.2초 ✓ / 친구 4명 배치 ✓.
그리고 자랑하고 싶은 것으로 **손전등 risk-reward**를 꼽았다 — §M6의 ×1.35 트레이드오프를
지켜낸 게 맞았다는 증거다. 절대 건드리지 말 것.

---

### 12-A. 「좀비가 다 똑같이 생겼다」 — **수락 (조건부)**

이전에 거절한 것은 **모자·이름**이었고, 그 이유는 "적은 한눈에 구분돼야 한다"였다.
그 판단은 지금도 유효하다. 하지만 아이가 말한 건 "좀비를 구분하고 싶다"가 아니라
**"복제 인간처럼 보인다"**이다. 이건 다른 문제고, 실루엣을 안 건드리고도 고칠 수 있다.

**절대 조건 — 흑백 실루엣 테스트**
> 아래 변주를 전부 적용한 기본 좀비 4마리를 **검정 실루엣으로만** 그렸을 때
> 서로 구분되지 않아야 하고, 배고픈 좀비·아주머니·교장과는 **한눈에 구분되어야** 한다.
> 이 테스트를 통과하지 못하는 변주는 넣지 않는다.

**넣을 것 (기본 좀비 `z` 에만 적용. 배고픈 좀비·아주머니·교장·친구는 제외):**

| # | 변주 | 왜 안전한가 |
|---|---|---|
| 1 | **애니메이션 위상 분리** — 좀비마다 뒤뚱임·하품 주기에 고정 오프셋(0~2π)과 진폭 배율(×0.85~1.15)을 준다 | 형태도 색도 안 바뀐다. **"다 똑같다"는 인상의 실제 원인은 대부분 다 같은 박자로 흔들리는 것이다.** 가장 싸고 효과가 크다 |
| 2 | **하품 말풍선 문구 3종 로테이션** — `하암…` / `숙제…` / `zzz` (좀비별 고정) | 말풍선은 이미 있다. 글자만 바꾼다. 스토리(`"숙제… 숙제…"` 중얼거림)와도 맞는다 |
| 3 | **앞머리 3종** — 몸통 캡 **윤곽선 안쪽에만** 그리는 어두운 아치 (일자 뱅 / 옆가르마 / 뿔 두 개) | 윤곽 밖으로 한 픽셀도 나가지 않으므로 실루엣이 동일하다. 색은 **몸 색을 어둡게 한 단일 톤**만 쓴다 — 새 색을 추가하지 않는다 |

**넣지 말 것:** 모자, 이름표, 몸 색 변주, 눈 색 변주, 크기 변주, 소품(가방·안경 등).
전부 실루엣이나 색으로 "이건 다른 종류의 적"이라는 잘못된 신호를 준다.

`?`/`!` 때 눈이 커지는 연출은 이미 들어가 있다(graphic-designer 작업분). 그대로 둔다 —
**표정은 "상태"를 나타내고, 위 3종은 "개체"를 나타낸다.** 두 축이 섞이지 않는 게 중요하다.

담당: graphic-designer (`sprites.ts`의 `drawZombie`). 좀비 개체마다 고정 시드가 필요하니
`engine.ts`의 좀비 생성 시 `variant: 0|1|2`와 `phase: number`를 한 번만 뽑아 들고 있으면 된다.

---

### 12-B. 「사물함 안이 안 보여서 무섭다」 — **수락 (인식 문제, 시각으로 해결)**

사물함은 **스펙상 항상 100% 안전하다**(§M2). 규칙을 바꿀 이유는 없다.
문제는 규칙이 아니라 **그 사실이 화면에 안 적혀 있다**는 것이다.
그리고 이건 그냥 기분 문제가 아니다 — 아이가 숨기를 망설이면 1-3부터 5-3까지
설계의 절반이 안 쓰인다. **고쳐야 한다.**

고칠 것:
- 상호작용 대상으로 잡힌 사물함(가장 가까운 하나, 34px 이내)의 문이
  **약 15% 열린 채로 그려진다.** 안쪽에 **따뜻한 크림색 빈 공간**(`#FFE9B8` 계열)이 보인다.
- 지금 있는 `↓` 아이콘과 청록 하이라이트 테두리는 그대로 둔다.
- 여러 개가 가까이 있어도 **열리는 건 실제 대상 하나뿐이다.** 어느 칸에 들어갈지도 같이 알려준다.

왜 이 방식인가: 새 UI를 하나도 안 늘리고, **"안이 비었다 = 들어가도 된다"가 그림 하나로 전달된다.**
그리고 §M2가 이미 쓰고 있는 언어 — "숨으면 문틈으로 밖이 보인다" — 의 정확한 반대편이라
배우는 데 설명이 필요 없다.

담당: graphic-designer (`drawLockerTile`에 `highlighted` 상태 추가).

---

### 12-C. 「엔딩에 친구 4명이 다 안 보인다」 — **이미 구현됨. 다만 빈자리를 보이게 고친다**

먼저 사실관계: **구현돼 있다.** `EndingScreen.tsx`가 `FRIENDS`를 돌면서 구출한 친구를
`drawFriend`로 그리고, 그 친구의 대사(`f.line`)도 엔딩 대사 줄에 넣는다.
아이가 본 화면은 **아무도 구출하지 않은 테스트 런**이었을 가능성이 높다.

그런데 코드를 보면 진짜 문제가 하나 있다:

```
FRIENDS.forEach((_, i) => { if (!friendsMet[i]) return; ... });
const met = friendsMet.filter(Boolean).length;
const slots = met + 1;
```

**못 구한 친구는 자리 자체가 사라지고 레이아웃이 다시 가운데 정렬된다.**
그래서 0명이면 주인공만 덩그러니 서 있고, 아이 눈에는 "친구가 안 나온다 = 버그"로 보인다.
빈자리가 안 보이면 "다시 해서 채워야지"라는 동기도 안 생긴다 —
§7에 적어 둔 `"못 구한 친구는 그 자리가 비고, 다시 플레이할 이유가 된다"`가 작동하지 않는다.

고칠 것 (`EndingScreen.tsx`):
- **슬롯은 항상 5개**로 고정한다: 주인공 1 + 친구 4. `slots = 5`, 레이아웃 재정렬 없음.
- 구출한 친구: 지금처럼 컬러 + 하늘색 글로우 + 손 흔들기.
- **못 구한 친구: 같은 자리에 회색 실루엣으로 누워서 `zzz`** (급식표 수집 화면의 회색 실루엣과 같은 언어).
  머리 위에 그 친구 이름을 작게 띄운다.
- 엔딩 대사 줄에 한 줄 추가 — 못 구한 친구가 있을 때만:
  `"아직 자고 있는 친구가 {n}명 있대. 다시 가 볼까?"`
- 4명 전원 구출 시: 다섯이 나란히 서서 손 흔들고, 한 줄 추가 —
  `"{이름}이랑 친구들 넷, 다 같이 학교를 구했다!"`

이러면 아이의 요구("4명이 다 보였으면")와 §7의 설계(빈자리가 재플레이 동기)가 동시에 충족된다.
**보이긴 다 보이되, 안 구한 애는 자고 있다.**

---

### 12-D. 그 밖

| 관찰 | 판정 |
|---|---|
| 아이가 옷색을 **8종**으로 셌다 (스펙은 4종) | 빌더가 늘린 것. **그대로 둔다** — 커스터마이징은 많을수록 좋고 게임플레이 legibility와 무관하다. 부록 A는 건드리지 않는다(플레이어는 화면에 한 명뿐이라 구분 문제가 없다) |
| 아이가 좀비를 `"귀여운 파란 사물함 + 눈"`이라고 묘사 | graphic-designer 참고용. 몸통이 캡슐이 아니라 **각진 상자로 읽히고 있다.** §12-A 작업할 때 캡슐의 위·아래 모서리 라운드를 키우는 걸 같이 검토할 것. (실루엣 변경이므로 사물함 타일과 안 헷갈리는지 확인 필수 — 아이가 실제로 사물함에 빗댔다는 게 경고 신호다) |
| `"뮤트해도 화살표·불빛·게이지로 다 알 수 있어서 좋아요"` | **§9-0 규칙이 실전에서 통했다는 증거.** playtester의 뮤트 검증(항목 5)이 끝나기 전이지만 좋은 신호다. 규칙 유지 |
| playtester [사소] `friendsMet`가 `null`로 저장되는 버그 | 표시에는 영향 없다(`null`도 falsy). 다만 **§12-C에서 슬롯을 5개로 고정하면 `friendsMet[i]`를 그대로 인덱싱하게 되므로** `boolean[]` 4칸을 항상 보장하도록 같이 고칠 것 |

### 12-E. 우선순위

1. **12-C 엔딩 슬롯 5개 고정** — 아이가 "버그다"라고 느낀 유일한 항목. 제일 먼저.
2. **12-B 사물함 문 살짝 열기** — 설계의 절반이 안 쓰이는 걸 막는다.
3. **12-A 좀비 변주 3종** — 9점을 10점으로 만드는 항목. 흑백 실루엣 테스트를 반드시 통과할 것.

---

## 13. v2 범위 (v0.5)

v1은 라이브(https://06schoolzombieletssurvive.vercel.app), playtester 통과, audience 9/10.
후보는 `docs/IDEAS_V2.md` 5개(6번 연습 모드는 이미 구현됨) + 아이가 직접 낸 「친구 휘슬」.

아이 순위: 🥇환풍구 → 🥈친구 특기 → 🥉푸딩 미끼 → 기록판 → 데일리.
"친구한테 자랑하고 싶은 것"으로도 **환풍구**를 꼽았다. 그게 이번 라운드의 중심이다.

### 채택 3 + 저비용 1

| # | 기능 | 한 줄 | 커브 진입 지점 |
|---|---|---|---|
| **V1** | **환풍구 비밀 통로** | 순찰 구간 하나를 통째로 우회하는 **발견형** 지름길. 절대 필수가 아니다 | **어느 구역도 새 요소로 쓰지 않는다** — 전 구역에 흩어진 선택 요소 (§13-1 참조) |
| **V2** | **친구 특기 + 5스테이지까지 동행** | 친구 4명이 각자 작은 능력을 주고, **스테이지 5까지 따라온다** | **4-2**(친구 구출 구역의 확장) → 효과는 4-3·5-1~5-3에서 발휘 |
| **V3** | **두 자매 기록판** | 이름별 스테이지 최고 점수·최단 시간을 나란히 표시 + 기록 갱신 알림 | 커브 밖 (스테이지 선택 화면) |
| — | 푸딩 미끼 · 데일리 챌린지 · 휘슬 버튼 | **뺀다** (§13-4) | — |

---

### 13-1. V1 — 환풍구 비밀 통로

#### 이 기능이 실패하는 조건부터 정한다

> **환풍구가 사물함과 분필을 밀어내면 실패다.**
> 사물함은 "쫓길 때 살아남는 곳", 분필은 "시선을 돌리는 것", 환풍구는 **"애초에 마주치지 않는 길"**이다.
> 세 개의 역할이 겹치는 순간 이 기능은 뺀다.

역할이 안 겹치게 만드는 장치는 **딱 하나, 진입이 느리고 그동안 무방비라는 것**이다.

| 항목 | 값 |
|---|---|
| 진입 상호작용 | **0.8초** (사물함은 0.25초). 도중에 움직이면 취소 |
| 이동 (관 속) | **1.2초** — 먼지 날리는 어두운 크롤 연출, 조작 불가 |
| 출구 등장 | **0.3초** |
| **총 소요** | **2.3초** |
| 크롤 중 바깥 세상 | **계속 흐른다.** 시간까지 멈추면 환풍구가 스펙보다 세진다. 그동안 플레이어는 보이지 않고 잡히지도 않는다 |
| **등장 유예** | 나온 직후 **0.6초간 잡히지 않는다.** 근처 좀비는 반드시 `?`부터 시작한다 — **환풍구에서 나오자마자 즉사하는 경우는 없다** |
| **진입 금지 조건** | **추격(`chase`) 상태인 좀비가 200px 안에 있으면 못 들어간다.** 캐릭터가 거부한다 — `"지금은 안 돼!"` |
| 재진입 쿨다운 | **2.0초** (아무 환풍구나. 핑퐁 방지) |
| 진입을 목격당하면 | 좀비가 환풍구 앞에서 **3.5초** 하품하다 순찰 복귀 (사물함과 동일) |

→ **쫓기는 중에는 절대 못 쓴다.** 그러면 사물함의 자리는 안전하다.
→ 시선을 돌리지도 않는다. 그러면 분필의 자리도 안전하다.
→ 걸어서 6~10초 걸리고 순찰과 마주쳐야 하는 길을 **2.3초 + 무위험**으로 바꾼다. 그게 전부이고 충분하다.

#### 개수와 위치

**스테이지당 1 / 2 / 2 / 2 / 1 = 총 8쌍.** 한 쌍은 **인접한 두 방**만 잇는다.
맵을 가로지르는 장거리 환풍구는 없다 — **순찰 구간 하나를 건너뛰는 것이지 스테이지를 건너뛰는 게 아니다.**

| 스테이지 | 쌍 | 입구가 숨는 곳 | 무엇을 우회하나 |
|---|---|---|---|
| 1 | 1 | 급식실 **배식구 뒤** ↔ 보건실 **침대 뒤** | 중앙 복도 횡단(복도 좀비 1마리) |
| 2 | 2 | ① 4학년 1반 **사물함 옆** ↔ 4학년 3반 **청소도구함** ② 복도 끝 ↔ 도서 준비실 | ① 아주머니의 복도 순찰 1회 ② 복도 끝 노출 구간 |
| 3 | 2 | ① 음악실 **큰북 뒤** ↔ 미술실 **이젤 뒤** ② 미술실 구석 ↔ 도서관 서가 뒤 | ① 밝은 방→어두운 방 진입 노출 ② 도서관 입구 병목 |
| 4 | 2 | ① 체육관 **뜀틀 뒤** ↔ 체육 창고 ② **스탠드 밑** ↔ 운동장 구석 | ① 체육관 중앙 횡단 ② 운동장 진입 직후의 무방비 구간 |
| 5 | 1 | 방송실 앞 **좌측 구석 ↔ 우측 구석** (같은 방 안) | 5-3에서 좀비 4마리 사이를 가로지르는 것 |

**보스 층 규칙 (명시):**
> **어떤 환풍구도 교장 복도(5-2)를 우회하지 않는다.**
> 5스테이지의 유일한 쌍은 **방송실 앞 방 안에서만** 이어진다. 5-2는 이 게임의 클라이맥스이고,
> 연설 리듬을 배우는 구간이다. 그걸 건너뛰게 하면 보스전이 사라진다.
> 또한 **5-3에서 방송 게이지를 실제로 충전하는 동안(마지막 충전 250ms 이내)에는 환풍구에 못 들어간다.**
> 손을 떼고 걸어가서 환풍구를 타는 것은 **허용된다** — 게이지는 줄지 않으므로 손해는 아니고,
> 그저 시간을 버리는 선택이 된다. 두 상호작용 대상은 서로 다른 자리에 있어 실제로는 거의 겹치지 않는다.

**운동장(4-3)에 환풍구가 있어도 "숨을 곳이 거의 없다"는 설계는 안 깨진다** —
환풍구는 숨는 곳이 아니다. 쫓기면 못 들어간다. 4-3의 답은 여전히 알람시계와 친구 미끼다.

#### 발견 방법 — **상호작용. 걸어 들어가는 게 아니다.**

- 미발견 환풍구는 **그냥 배경 소품**으로 그려진다 (벽에 붙은 환기 그릴). **하이라이트 없음.**
- 34px 안에 들어가면 다른 소품과 똑같이 상호작용 프롬프트가 뜬다 → 누르면 **발견**.
- 왜 상호작용인가:
  1. 걸어 들어가면 **도망치다 실수로** 들어간다. 그건 §13-1의 "쫓길 때 못 쓴다" 규칙과 정면충돌이고,
     조작을 뺏긴 느낌을 준다.
  2. 누르는 행위가 있어야 **"내가 찾았다"**가 된다. 아이가 자랑하고 싶어 한 건 그 순간이다.
  3. 이미 있는 문맥 상호작용 시스템(§2)에 대상 하나만 추가하면 된다.

**어두운 방(3-2, 3-3)의 환풍구는 손전등을 켜야만 보인다.**
어둠 속에서 그릴은 벽과 구분되지 않는다. → 손전등을 켜면 감지 ×1.35(§M6).
**호기심에 값을 매기는 것이다.** 새 규칙을 안 만들고 기존 트레이드오프를 재사용한다.

#### 미니맵·저장·수집

- **미발견: 미니맵에 안 나온다.** 발견: **청록 마름모**로 영구 표시.
- 발견 상태는 `localStorage`에 **스테이지별로 영구 저장**. 한 번 찾으면 영원히 내 것이다.
- HUD 급식표 카운터 옆에 **`환풍구 3/8`**. 급식표와 같은 수집 언어.
- 발견 1개당 **+200점**. **8/8 전부 찾으면 모자 「청소부 모자」 해금.**
- 스테이지 선택 화면의 스테이지 카드에 작은 마름모 n/m 표시 → "여기 아직 하나 남았네"가 보인다.

#### 좀비는 환풍구를 쓰지 않는다 — **확정**

> 좀비가 환풍구에서 튀어나오면 **`?` 0.6초 유예로 지탱하는 공정성 계약(§M1)이 깨진다.**
> "갑자기 나타나는 적"은 이 게임이 처음부터 배제한 것이고, audience가 v1에서
> "갑자기 튀어나오는 좀비는 스트레스"라고 명시했다.
> 그리고 아이가 원한 건 **비밀** 통로다. 좀비가 알면 비밀이 아니다.

#### 사운드 (musician)

`ventOpen` (금속 그릴 삐걱, 0.4초) / `ventCrawl` (관 속 울리는 기어가는 소리 + 먼지, 1.2초 루프) /
`ventFound` (발견 순간 — 급식표 `paper`보다 낮고 뿌듯한 3음 상승).
**정보성 소리 아님** → §9-0 시각적 쌍 불필요. 크롤 연출 자체가 화면에 다 나온다.

---

### 13-2. V2 — 친구 특기 + 스테이지 5 동행

#### 먼저 고칠 구조적 문제

아이는 `"매 게임마다 누가 따라오는지 달라지는 거 좋아함"`이라고 했는데,
**v1에서 친구 4명은 전부 스테이지 4에 있고 그 스테이지가 끝나면 사라진다.**
즉 특기를 붙여도 **쓸 시간이 4-2~4-3밖에 없다.** 그래서 특기만 얹으면 체감이 거의 없다.

> **구출한 친구는 스테이지 5까지 따라온다.**
> 5스테이지 시작 시 `friendsMet` 기록에 있는 친구가 뒤에 줄줄이 붙어서 등장한다.
> 세은의 대사 `"나도 방송실 가는 거 도와줄게!"`가 그제서야 말이 된다.

**보스 안전장치 (필수):**
- **교장의 연설은 친구를 표적으로 삼지 않고, 친구는 연설에 면역이다** (옆에서 같이 하품만 한다).
- 친구는 **일반 좀비 4마리에 대해서만** 미끼로 작동한다.
- → 보스전의 난이도 축(연설 반경 · 기둥 · 안전 창)은 **그대로**다. 친구는 곁다리 위협만 덜어 준다.

#### 특기 4종 — 전부 **누적된다(stack)**

작고, 눈에 보이고, 기존 도구를 죽이지 않는 것만 골랐다.

| 친구 | 특기 | 정확한 수치 | 왜 이 친구인가 / 왜 안전한가 |
|---|---|---|---|
| **준호** (먹보) | **급식표 조각을 미니맵에 표시** | 같은 스테이지의 미발견 급식표 2개를 **노란 종이 아이콘**으로 상시 표시 | §M11은 급식표를 일부러 미니맵에서 뺐다("현장에서 찾는 재미"). **그 규칙을 푸는 것이 곧 보상**이 된다. 먹보가 급식표 위치를 아는 건 설명이 필요 없다 |
| **다온** (미술부) | **분필 쿨다운 단축** | **1.2초 → 0.7초** | 분필을 죽이는 게 아니라 **분필을 더 쓰게 만든다.** "도구를 유지하라"는 이번 라운드 원칙에 정확히 맞는 방향 |
| **세은** (방송부) | **방송 게이지 충전 ×1.5** | 5-3 누적 3.0초 → **2.0초** | 안전 창 하나(2.2+5.5초) 안에 더 많이 채운다. 보스 사이클 자체는 안 건드린다 |
| **하늘** (축구부) | **`?` 유예 +0.2초** | 스테이지 4·5에서 0.5초 → **0.7초** | 이 게임의 공정성 신호를 직접 늘려 준다. 가장 체감이 크고 가장 안 위험하다 |

**4명 다 모으면**: 급식표가 보이고, 분필이 두 배로 나가고, 방송이 빨리 차고, 반응할 시간이 더 있다.
**어느 것도 좀비를 직접 무력화하지 않는다.** 전부 "조금 편해진다"의 범주다 — 그래서 스택해도 안 깨진다.

#### 아이 요청: `"능력을 더 눈에 띄게 표시해 줬으면"`

- 따라오는 친구 **머리 위에 특기 아이콘 상시 표시** (종이 / 분필 / 마이크 / 시계).
- **특기가 실제로 발동하는 순간 아이콘이 팝**한다:
  다온은 단축된 쿨다운으로 분필을 던질 때마다, 세은은 게이지가 찰 때마다,
  하늘은 `?`가 뜰 때 `?` 옆에 작은 `+0.2` 가 뜬다, 준호는 급식표 아이콘이 미니맵에 켜질 때 한 번.
- ~~하단 HUD 띠의 동행 친구 아이콘 줄~~ → **삭제.** 390px에서 재료 8칸·구역 이름과 자리를 다퉜다 (§14-5).
  머리 위 아이콘만으로 충분하다.

#### 아이 아이디어 「친구 휘슬」 — **버튼은 안 만들고, 필요는 채운다**

그 아이가 지적한 문제는 진짜다: 스테이지 4는 44×30에 줌아웃 0.8이라 **친구를 못 찾는다.**
하지만 버튼으로 풀면 안 된다 — 390px 화면에 이미 버튼이 4개고,
**부작용 없이 정보만 켜 주는 버튼은 그냥 항상 켜 두는 게 맞다.**

- **아직 못 구한 친구는 260px 안에서 2.5초마다 `"여기야!"` 말풍선**을 띄운다.
- **300px 안에서 미니맵에 하늘색 점**으로 보인다 (친구 글로우와 같은 `#7FD4FF`).
- 버튼 없음. 입력 없음. 넓은 방 문제만 정확히 해결된다.

#### 커브 진입

**4-2 「친구 구출」 구역의 확장이다. 새 요소가 아니다.**
그 구역이 가르치는 것은 여전히 "친구를 구해서 데리고 다닌다" 하나이고,
특기는 그 요소에 붙는 성질이지 별도의 규칙이 아니다. → **구역당 새 요소 하나 규칙은 유지된다.**
효과가 실제로 발휘되는 곳은 4-3과 스테이지 5 전체다.

---

### 13-3. V3 — 두 자매 기록판 (저비용 4번째)

아이 평가는 ⭐⭐⭐로 낮지만 `"엄마랑 언니 이름 있으면 보긴 할 것 같음"`,
`"이번엔 내가 더 빨리! 같은 경쟁 보너스가 있으면"`이라고 했다.
**자매가 같은 기기로 번갈아 하는 게 이 게임의 실제 사용 환경**이므로 넣는다.

**범위를 좁게 못 박는다 — 프로필 시스템을 만들지 않는다.**
- 진행도(해금·별·급식표·환풍구·모자)는 **지금처럼 하나를 공유**한다.
- 추가되는 건 **기록 칸 하나뿐**: `records: { [이름]: { best: number[5], bestTimeMs: number[5] } }`
- 타이틀에서 이름을 바꾸면 **어느 칸에 기록할지가 바뀔 뿐**이다. 마이그레이션 부담 없음.

표시:
- 스테이지 선택 화면의 각 스테이지 카드에 **이름 두 개를 위아래로** (최근 플레이한 두 이름).
  각 줄에 `최고점 / 최단시간`.
- 각 스테이지의 1등 이름 옆에 **작은 왕관**. 게임플레이 효과 없음.
- **기록을 깨는 순간 토스트**: `"{상대이름}의 기록을 깼다!"` — 아이가 요청한 경쟁 보너스가 이것이다.
  점수 보너스는 주지 않는다 (혼자 하는 아이가 손해 보면 안 된다).
- 이름이 하나뿐이면 그냥 한 줄만 나온다. 처음 켜는 아이에게 빈 칸을 보여주지 않는다.

---

### 13-4. v2에서 뺀 것

| 뺀 것 | 이유 |
|---|---|
| **푸딩 미끼** | **알람시계와 역할이 겹친다.** 알람은 이미 "반경 220px의 좀비를 4초간 한자리에 붙잡는다"이고, 푸딩은 "반경 안 좀비를 모아서 N초 재운다"이다 — 더 센 알람시계일 뿐이라 알람시계가 죽는다. 아이 본인도 `"그냥 도망다닐 수도 있잖아?"`, `"꼭 써야 할 것처럼 안 느껴짐"`이라고 했다. 게다가 이번 라운드는 이미 **환풍구라는 새 이동 수단**을 넣는다 — 한 라운드에 "이 상황을 어떻게 뚫지?"의 새 답을 둘씩 주면 배우는 리듬이 깨진다. **v3으로 넘긴다. 그때는 "더 센 알람"이 아니라 「모아서 문 잠그기」 콤보 전용 도구**(§M5 문 잠그기가 지금 가장 안 쓰이는 메커닉이다)로 다시 설계한다 |
| **「오늘의 급식」 데일리 챌린지** | 아이: `"매일은 아닐 것 같음"`, `"모자가 얼마나 멋진지가 중요함"`. 지금 모자는 순수 장식이라 보상이 약하다. **보상이 약한 반복 장치는 빈 껍데기**라는 v0.2 §7의 판단 그대로. v3에서 모자에 실제 매력(엔딩 컷 변화 등)이 생긴 뒤에 붙인다 |
| **「친구 휘슬」 버튼** | 아이디어의 **필요는 채택**했다(§13-2 마지막). 버튼만 거절한다 — 390px에 버튼이 이미 4개이고, **부작용 없이 정보만 주는 버튼은 항상 켜 둔 정보와 같다.** 누를 이유가 100%인 버튼은 버튼이 아니다 |
| **좀비의 환풍구 사용** | `?` 유예로 지탱하는 공정성 계약이 깨진다. audience가 v1에서 갑툭튀를 스트레스로 지목했다. 그리고 비밀 통로는 비밀이어야 한다 |
| **교장 복도를 우회하는 환풍구** | 5-2는 보스의 리듬을 배우는 구간이다. 우회로가 있으면 보스전이 사라진다 |
| **환풍구를 걸어 들어가서 발견** | 도망 중 오발동한다. "쫓길 때 못 쓴다"는 핵심 규칙과 충돌하고, 발견의 쾌감도 사라진다 |

---

### 13-5. 우선순위와 검증

**만드는 순서**: V1 환풍구 → V2 친구 특기 → V3 기록판.
V1이 이번 라운드의 얼굴이고, V3은 언제 붙여도 되는 곁가지다.

**빌드 후 반드시 확인할 것 (playtester):**
1. **환풍구를 알고도 사물함을 계속 쓰는가.** 스테이지 3~4에서 사물함 사용 횟수가 v1 대비 줄지 않아야 한다.
   줄면 진입 0.8초를 1.1초로 올린다.
2. **환풍구 8개 중 몇 개를 스스로 찾는가.** 4개 미만이면 미발견 그릴의 대비를 아주 살짝 올린다
   (하이라이트는 여전히 안 준다).
3. **어두운 방 환풍구를 손전등으로 찾아내는가.** 못 찾으면 3스테이지 환풍구 위치를 벽 쪽으로 옮긴다.
4. **친구 4명 스택 상태의 스테이지 5가 너무 쉬워지지 않는가.** 방송 2.0초 + `?` 0.7초가 겹친다.
   쉬우면 세은을 ×1.5 → ×1.3으로.
5. **기록 갱신 토스트가 혼자 하는 아이에게 안 뜨는지** 확인 (이름이 하나면 비교 대상이 없다).

---

## 14. v2 빌더 편차 판정 (v0.6)

`BUILD_NOTES.md` §2-5의 편차 4건 + 스크린샷 12장 검토.
환풍구 8쌍·특기 4종·기록판 전부 §13대로 들어갔고, 특히 **다온 632ms / 하늘 +200ms / 세은 1500 대 1000**
같은 실측치를 붙여 온 게 좋다. 숫자로 검증된 스펙은 다시 논쟁할 필요가 없다.

### 14-1. `"여기야!"`가 `friendWake` 소리를 재사용한다 — **반려 (경미)**

- 문제: `friendWake`는 **"친구가 깨어났다"는 1회성 사건**이고, `"여기야!"`는 **2.5초마다 반복되는 위치 비컨**이다.
  같은 소리를 쓰면 아이는 구출 전 내내 "깨어남" 소리를 듣게 되고, **정작 진짜 구출한 순간이 안 특별해진다.**
- 고칠 것 — 전용 `friendCall` 신설 (musician):
  - sine **660 → 880Hz**, 길이 **180ms**, attack 10ms / decay 140ms
  - 음량은 `friendWake`의 **×0.5** (배경에 깔리는 비컨이지 알림이 아니다)
  - **`yawn`과 같은 정위 처리**: `StereoPanner`로 좌우, `gain = max(0, 1 - dist/260)`
  - `friendWake`(392→523→659Hz, 500ms)보다 **짧고 높다** → "부르는 소리"로 읽힌다
- §9-0 관련: 이 소리는 정보성이지만 **시각적 쌍이 이미 있다**(말풍선 + 미니맵 하늘색 점). 뮤트 클리어에는 영향 없음.

### 14-2. 방송 충전 중에만 환풍구 금지 — **수락**

§13-1의 의도를 정확히 읽었다. 내가 쓴 `"시간을 버리는 선택이 되게 둔다"`가 곧
"손 떼고 걸어가는 건 허용"이라는 뜻이었다. **스펙 문구를 코드에 맞춰 정밀화했다**(마지막 충전 250ms 이내).

### 14-3. 스테이지 2 vent ①이 「사물함 옆」 → 청소도구함 — **수락 (조건부)**

- 스펙의 "사물함 옆"은 **분위기 묘사**였고 진짜 요구는 "눈에 안 띄는 곳"이었다. 청소도구함이 오히려 낫다.
- **다만 조건 하나** — §8 규칙 2("숨을 수 있는 것에만 청록 하이라이트")를 지켜야 한다.
  청소도구함은 **숨을 수 있게 생겼다.** 아이가 들어가려다 안 되면 그게 legibility 실패다.
  - 환풍구용 청소도구함 문에는 **가로 루버(살) 무늬를 반드시 넣는다** — "이건 통풍구다"가 형태로 읽혀야 한다.
  - **청록 테두리(사물함 전용)는 절대 쓰지 않는다.** 발견 후 표시는 **청록 마름모 마커**로만.
- 담당: graphic-designer.

### 14-4. 크롤 2.3초 동안 세상이 계속 돈다 — **수락. 단, 등장 즉사는 막는다**

- 빌더 판단이 맞다. 시간을 멈추면 환풍구는 "무적 2.3초 + 순간이동"이 되어 §13-1이 막으려던 바로 그것이 된다.
  세상이 도는 건 **비용**이다 — 순찰이 움직이는 동안 나는 대응할 수 없다. 그 긴장이 있어야 맞다.
- **다만 딱 한 가지 불공정한 경우가 열려 있다**: 크롤 1.2초 사이에 좀비가 출구 앞으로 걸어오면
  **`?` 유예 없이 0px에서 잡힌다.** 이건 §M1의 공정성 계약(`?` 0.5~0.6초 유예) 위반이고,
  아이 입장에선 "내가 뭘 잘못했는지 모르는 죽음"이다. 이 게임에서 가장 피해야 하는 실패다.
- 고칠 것 (§13-1 표에 반영함):
  - **나온 직후 0.6초간 잡히지 않는다.**
  - 그 사이 근처 좀비는 **반드시 `?`부터 시작한다.** 등장 → 즉시 추격 → 즉시 잡힘 경로를 없앤다.
  - 시간 정지가 아니다. 그냥 **등장 유예 0.6초**다.

### 14-5. 스크린샷 검토 — **하단 HUD 띠가 실제로 무너져 있다 (내 스펙 잘못)**

`v2-friend-powers.png`, `v2-stage5-companions.png` (390×844) 확인 결과:

| 증상 | 근거 |
|---|---|
| **분필·손 버튼이 재료 8칸 줄을 덮고 있다.** 재료 줄이 화면 맨 아래에 깔려 버튼 뒤로 들어감 | `v2-friend-powers.png` 하단 — 분필 버튼이 6번 칸, 손 버튼이 7~8번 칸 위 |
| **구역 이름이 세로로 쪼개져 읽을 수 없다** — `4-1 체육관`이 `4 / 1 / 체 / 육` 으로 줄바꿈되어 우측 끝에 잘림 | 같은 이미지 우하단 |
| **동행 친구 아이콘 3개가 우측 자투리에 초소형으로 끼어 있다** | 같은 이미지, 손 버튼 아래 |

원인은 빌더가 아니라 **나다.** §13-2에서 이미 꽉 찬 40px 띠에 동행 아이콘 줄을 더 얹으라고 썼다.
40px 안에 재료 8칸 + 친구 4칸 + 구역 이름은 390px에서 들어가지 않는다.

**고칠 것 (스펙 변경 3개):**
1. **동행 친구 아이콘 줄 삭제.** (§13-2 수정 완료) 친구 **머리 위 아이콘**은 이미 잘 보인다
   (`v2-stage5-companions.png`에서 종이·분필·시계·마이크가 또렷하다). 중복이었고, 그 중복이 구역 이름을 죽였다.
2. **구역 이름을 상단 띠로 옮긴다.** 정적인 텍스트다. `급식표 n/10` · `환풍구 n/8` 옆/아래 줄로.
3. **재료 8칸 줄을 화면 맨 아래가 아니라 캔버스 바로 아래에 붙인다.**
   버튼은 화면 아래에서 20~200px에 뜨므로, 띠가 캔버스 바로 밑(상단에서 52+520=572px 지점)에 있으면
   **절대 안 겹친다.** 세로 예산: 52(상단) + 520(캔버스) + 40(재료 줄) = 612, 컨트롤에 232px 남음. 들어간다.

부록 A의 `HUD 상단 / 하단 재료줄 (390px 기준) | 52px / 40px`는 그대로 유효하다. **위치만 바뀐다.**

### 14-6. 그 밖 (스크린샷)

- **기록판은 좋다** (`v2-records-board.png`). 왕관·환풍구 n/m·두 줄 기록 전부 §13-3대로.
  **작은 것 하나**: 지금은 1등(서아)이 밝고 **현재 플레이 중인 이름(하린)이 회색**이라 자기 줄을 못 찾는다.
  → **현재 이름 줄에 옅은 배경 칩**을 깔아 준다. 왕관은 1등에 그대로 둔다. 둘은 다른 축이다.
- **스테이지 5 동행 4명이 서로 겹쳐 한 덩어리로 보인다** (`v2-stage5-companions.png`).
  `FRIEND_FOLLOW_DIST 32`가 4명 줄줄이에는 너무 촘촘하다.
  → **N번째 친구의 추종 거리를 `32 + 14 × (N-1)`** 로 (32/46/60/74). 뱀처럼 늘어서게 한다.
- 미발견 환풍구 그릴이 배경 소품으로 잘 묻혀 있다 (`v2-vent-undiscovered.png`). 의도대로다. 건드리지 말 것.
- 스크린샷 좌하단의 검은 `N` 원은 **Next.js 개발 배지**다. 게임 UI 아님 — 프로덕션에는 안 나온다. 무시.

### 14-7. 요약 — 빌더가 할 일

1. `friendCall` 전용 SFX 신설 (§14-1) — musician
2. 환풍구 **등장 유예 0.6초** + 등장 직후 `?` 강제 (§14-4) — **가장 중요. 불공정한 죽음을 막는다**
3. 하단 띠 재배치: 동행 줄 삭제 · 구역 이름 상단 이동 · 재료 줄을 캔버스 바로 아래로 (§14-5)
4. 환풍구 청소도구함에 루버 무늬, 청록 테두리 금지 (§14-3) — graphic-designer
5. 기록판 현재 이름 강조, 친구 추종 거리 32/46/60/74 (§14-6)

---

## 15. 난이도 선택 — 쉬움 / 보통 / 어려움 (v0.7)

**보통 = v0.6의 현재 숫자 그대로.** 한 글자도 안 바뀐다. 기존 저장 데이터는 전부 보통으로 취급한다.

두 가지 제약이 이 설계 전체를 지배한다:

> **쉬움**: 7살 동생이 해도 **스테이지 5까지 거의 질 수 없어야 한다.**
> **어려움**: 그래도 **공정해야 한다** — `?` 유예는 절대 0.35초 미만으로 안 내려가고,
> 플레이어(130 px/s)는 **언제나** 가장 빠른 추격자보다 빠르다.

### 15-0. 설계 원칙 세 가지

1. **보스 사이클 타이밍(5.5 / 1.0 / 3.5 / 2.2초)은 세 난이도에서 동일하다.**
   난이도를 바꿨다고 리듬을 다시 배우게 하면 안 된다. 바뀌는 건 **이동 속도와 반경**뿐이다.
2. **속도는 가장 무딘 손잡이다.** 어려움은 속도보다 **유예·감지 반경·포기 시간**을 주로 조인다.
   속도만 올리면 "억울하게 따라잡힘"이 되고, 그건 이 게임이 처음부터 배제한 실패다.
3. **구역 구성과 15구역 커브는 난이도와 무관하다.** 어느 난이도든 같은 순서로 같은 것을 배운다.

### 15-1. 마스터 표 (빌더용)

배수는 **보통 값에 곱한다**. 소수점은 반올림한 절대값을 같이 적었다.

| 항목 | 쉬움 | **보통 (=v0.6)** | 어려움 |
|---|---|---|---|
| `PATROL_SPEED` (S1→S5) | **×0.75** → 34/36/39/42/42 | 45/48/52/56/56 | **×1.15** → 52/55/60/64/64 |
| `CHASE_SPEED` (S1→S5) | **×0.72** → 53/56/59/63/65 | 74/78/82/88/90 | **×1.12** → 83/87/92/99/101 |
| `NOTICE_R` (S1→S5) | **×0.75** → 75/79/83/90/90 | 100/105/110/120/120 | **×1.20** → 120/126/132/144/144 |
| `NOTICE_DELAY` (S1→S5) | **+0.35초** → 0.95/0.90/0.85/0.85/0.85 | 0.60/0.55/0.50/0.50/0.50 | **−0.15초, 하한 0.35** → 0.45/0.40/**0.35/0.35/0.35** |
| `GIVEUP_MS` (S1→S5) | **×0.6** → 2100/2400/2400/2700/2700 | 3500/4000/4000/4500/4500 | **×1.4** → 4900/5600/5600/6300/6300 |
| `CATCH_DIST` | **16** | 20 | **23** |
| `BOSS_CATCH_DIST` | **24** | 28 | **31** |
| `MATRON` 순찰/추격/감지 | **47 / 75 / 98** | 62 / 104 / 130 | **71 / 116 / 156** |
| `HUNGRY` 순찰/추격/감지 | **기본 좀비와 동일**(배고픈 좀비 없음) | 52 / 88 / 110 | **60 / 99 / 132** |
| 보스 `WALK_SPEED` | **24** | 34 | **40** |
| 보스 `SLEEP_R` | **150** | 200 | **230** |
| 보스 사이클 4구간 | **변경 없음** | 5500/1000/3500/2200 | **변경 없음** |
| `BROADCAST_HOLD_MS` | **2000** | 3000 | **3500** |
| `ALARM_MAX` | **5** | 3 | **2** |
| 스테이지 시작 알람 개수 | **보통 +2** | 스테이지별 기존값 | **보통 −1** (최소 1) |
| `CHALK_CD` | **800** | 1200 | **1500** |
| `DARK_VIEW_R` | **210** | 150 | **120** |
| `TORCH_NOTICE_MULT` | **1.20** | 1.35 | **1.45** |
| 좀비 구성 | 배고픈 좀비 → **일반 좀비로 강등** | §부록 A 그대로 | 스테이지 2~5에서 **일반 1마리를 배고픈 좀비로 교체** (총 마릿수 불변) |
| 전용 규칙 | **시야가 끊기면 추격 즉시 해제** (§15-2) | — | — |

**가드레일 (코드에 상수로 박을 것):**
- `CHASE_SPEED_CAP = 118`. 어떤 난이도·스테이지에서도 추격 속도가 이 값을 넘지 않는다.
  현재 최댓값은 어려움 아주머니 **116** — 플레이어 130 대비 **12% 여유**가 남는다.
- `NOTICE_DELAY_FLOOR = 0.35`. 어려움 스테이지 3~5가 정확히 이 바닥에 닿는다. **더는 못 내린다.**

### 15-2. 쉬움 전용 규칙 — 「모퉁이를 돌면 놓친다」

> **쉬움에서는 추격 중인 좀비가 시야(LOS)를 잃는 순간 추격이 즉시 해제된다.**
> (보통·어려움은 `GIVEUP_MS` 동안 마지막으로 본 지점까지 쫓아온다.)

7살의 본능은 **"벽 뒤로 숨는다"**이다. 이 규칙 하나가 그 본능을 항상 정답으로 만든다.
해제되면 좀비는 마지막 목격 지점까지 느긋하게 걸어갔다가 순찰로 돌아간다 — 화면상으론 똑같이 보이고,
아이는 "내가 잘 숨었다"고 느낀다.

쉬움이 거의 질 수 없는 이유를 스테이지 5 기준으로 확인해 두면:
교장 24 px/s(플레이어의 1/5.4) · 졸음 반경 150px(기둥 3개로 충분히 가려짐) ·
일반 좀비 추격 65 px/s(플레이어의 절반) · 유예 0.85초 · 잡힘 거리 16px · 방송 2.0초.
**의도적으로 서 있지 않는 한 안 잡힌다.**

### 15-3. 점수 · 별 · 기록

| 항목 | 결정 |
|---|---|
| 점수 배수 | 쉬움 **×1.0** · 보통 **×1.0** · 어려움 **×1.5** |
| 별 (`stars[5]`) | **난이도 공유.** 쉬움으로 깨도 별은 별이고 다음 스테이지가 열린다 |
| 해금·급식표·환풍구·모자 | **전부 공유.** 난이도로 콘텐츠를 잠그지 않는다 |
| 기록 (`records`) | **난이도별로 따로 저장**하고, **왕관은 같은 난이도끼리만 비교**한다 |

쉬움에 점수 벌점을 주지 않는 이유: 왕관을 난이도별로 나누는 순간 "쉬움으로 기록 빼앗기" 문제가
사라진다. 그러면 남는 벌점은 **어린 동생만 때리는 순수한 벌**이라 넣을 이유가 없다.
어려움 ×1.5는 벌이 아니라 **동경의 대상**으로 둔다.

저장 구조 변경 (§13-3의 `records`를 한 겹만 확장):
```
difficulty: "easy" | "normal" | "hard"        // 현재 선택. 없으면 "normal"
records: { [이름]: {
    lastDifficulty: "easy"|"normal"|"hard",   // 그 이름을 고르면 이 난이도로 자동 복귀
    easy:   { best:[5], bestTimeMs:[5] },
    normal: { best:[5], bestTimeMs:[5] },
    hard:   { best:[5], bestTimeMs:[5] },
} }
```
- **기존 저장 마이그레이션**: 지금의 `records[이름].best/bestTimeMs`를 `normal`로 옮기고
  `difficulty`는 `"normal"`로 채운다. 아무것도 잃지 않는다.
- `lastDifficulty`가 핵심이다 — **자매가 번갈아 할 때 이름만 고르면 각자의 난이도로 맞춰진다.**
  동생이 쉬움으로 하고 나갔다고 언니가 쉬움으로 시작하는 일이 없어야 한다.
- 기록판(§13-3)의 각 줄에 **작은 난이도 뱃지**(`쉬움`/`보통`/`어려움`)를 단다.
- (선택) 스테이지 카드에 **"어느 난이도까지 별 3개를 받았는지"** 뱃지 하나. 언니에게 줄 목표.

### 15-4. 선택 위치와 규칙

**스테이지 선택 화면, 연습 모드 상자 바로 위.** 이름/외모 화면이 아니다.

이유:
1. 이름·외모 화면은 **첫 실행에만** 지나가는 흐름이다. 난이도는 **아무 때나** 바꿀 수 있어야 한다
   (동생이 한 판 하겠다고 오는 건 언제든 일어난다).
2. 스테이지 선택 화면에는 이미 **연습 모드**라는 "이번 판 설정" 상자가 있다. 같은 자리에 두면 배울 게 없다.
3. 7살에게 게임을 보기도 전에 난이도를 고르게 하는 건 무의미하다.

규칙:
- 버튼 3개 가로 배치, 연습 모드 칩과 같은 크기·같은 언어. 현재 선택은 노란 테두리.
- 각 버튼 아래 한 줄 설명: `쉬움` → "좀비가 느리고 금방 포기해요" / `보통` → "원래 게임" /
  `어려움` → "빠르고 눈치가 빨라요 · 점수 ×1.5"
- **스테이지가 시작되면 그 판 동안 잠긴다.** 일시정지 메뉴에서는 못 바꾼다 (기록이 오염된다).
- 선택은 저장되고, **플레이 중 상단 띠에 작은 난이도 뱃지**를 상시 표시한다
  (부모가 지금 어느 모드인지 한눈에 알아야 하고, 기록이 헷갈리지 않아야 한다).
- 난이도를 바꿔도 **아무것도 지워지지 않는다.**

### 15-5. 커브·기존 스펙과의 관계

- §4의 15구역 구성, §11-C의 구역별 좀비 내역, §13의 환풍구 8쌍·친구 특기는 **난이도와 무관하게 동일**하다.
  어려움의 "일반 1마리 → 배고픈 좀비 교체"는 **마릿수를 안 늘리므로** §11-C 표와 부록 A가 그대로 맞는다.
- 어려움에서 배고픈 좀비를 **추가**하지 않고 **교체**하는 이유: 마릿수를 늘리면 맵 배치·미니맵 밀도·
  성능을 전부 다시 봐야 한다. 교체는 공짜이고, **눈에 보이는 위협**(급식판 든 실루엣)이 스테이지마다
  하나씩 생겨서 체감 난이도가 숫자보다 크게 오른다.
- §M1-b의 "배고픈 좀비 경로는 손으로 찍는다"는 스테이지 1에만 해당한다. 어려움의 교체분은
  기존 자동 생성 경로를 그대로 쓴다.

### 15-6. 빌드 후 확인 (playtester)

1. **쉬움으로 스테이지 5를 일부러 대충 플레이해서 클리어되는가.** 한 번도 안 자고 깨져야 정상이다.
   두 번 이상 자면 보스 `SLEEP_R`을 150 → 130으로.
2. **어려움 3~5스테이지의 유예 0.35초가 억울한가.** 억울하면 0.40으로 올린다 — **내리지는 않는다.**
3. **어려움 아주머니 116 px/s를 직선으로 따돌릴 수 있는가.** 못 따돌리면 `CHASE_SPEED_CAP`을 110으로.
4. **이름을 바꿀 때 `lastDifficulty`가 제대로 따라오는가.** 자매 교대 시나리오로 확인.
5. 기존 세이브가 있는 상태로 업데이트했을 때 **기록이 `normal`로 안전하게 이관되는가.**
