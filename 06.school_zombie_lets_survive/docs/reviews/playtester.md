# 플레이테스터 리포트 — 「학교 좀비, 살아남자!」

빌드: BUILD_NOTES.md 2차 갱신 / DESIGN.md v0.3 기준.
테스트 방법: `npm run dev`(포트 3411) + Playwright(Chromium) 자동화.
`window.__zombie`(dev 전용 디버그 훅, `GameCanvas.tsx`)로 상태를 직접 조작해
15구역을 전부 손으로 오래 걷지 않고도 각 메커닉을 검증했다. 키보드(데스크톱)와
`page.touchscreen`/`tap`(아이폰 뷰포트)을 둘 다 사용해 실제 입력 경로를 최대한 그대로 탔다.

이 문서는 테스트를 진행하며 **실시간으로 갱신**된다.

---

## 진행 상황

- [x] 1. 데스크톱 1280×800 키보드: 타이틀 → 이름 입력(정상/빈칸/7자+) → 외모 → 스테이지 선택 → 스테이지1 클리어 → 새로고침 저장 확인 → 스테이지2 해금 확인
- [x] 2. 아이폰 13 (390×844) 터치 전용 플로우 + 스크린샷
- [x] 3. 게임오버 → 잠들기 컷신 → 스킵 → 재시작 → 아이템 유지
- [x] 4. 스테이지 1~5 메커닉 스모크 테스트 (사물함/분필/알람/문잠금/어둠+손전등/친구구출/아주머니 화살표/보스 사이클/방송 게이지/엔딩)
- [ ] 5. 사운드 끄기 상태에서 3-3, 5-3 시각 신호 검증
- [ ] 6. 콘솔 오류 / 벽 끼임 / 사물함 끼임 / 좀비 벽 통과 / 프레임 드랍
- [ ] 7. 폰 가로 모드 안내 문구

---

## 버그 목록

### [보통] 데스크톱 와이드 화면에서 HUD 상단/하단 띠가 게임 캔버스와 정렬되지 않음

- **어디서**: 데스크톱 1280×800, 스테이지 플레이 화면 전체
- **재현 절차**:
  1. 1280×800 데스크톱 뷰포트로 접속
  2. 이름 입력 → 스테이지 1 진입
  3. 상단 점수/급식표/일시정지 줄과 하단 재료 8칸 줄을 확인
- **증상**: 게임 캔버스는 화면 가운데 좁은 박스(약 530×760)로 렌더링되는데,
  상단 띠(`h-[52px]`)와 하단 재료 줄(`h-[40px]`)은 `w-full`이라 뷰포트 **전체 폭(1280px)**으로 늘어난다.
  그 결과 점수(좌상단)·일시정지/음소거(우상단)·재료 8칸(좌하단)·구역 이름(우하단)이
  캔버스에서 한참 떨어진 화면 맨 가장자리에 붙는다. "급식표 n/10"만 우연히 화면 중앙이라
  캔버스 위쪽과 얼추 맞아 보이지만 나머지는 전부 어긋난다.
  DESIGN.md §5 "화면 표시"가 의도한 "정보 3개만 담은 상단 52px 띠"는 캔버스 폭 기준으로
  설계된 것으로 보이는데, 실제로는 페이지 폭 기준이라 넓은 화면에서 눈이 캔버스와
  HUD 사이를 오가야 한다.
- **증거**: `docs/screenshots/05-desktop-stage1-start.png`,
  `docs/screenshots/05b-top-strip.png`(상단 크롭), `docs/screenshots/05c-bottom-strip.png`(하단 크롭)
- **심각도**: 보통 — 플레이는 막히지 않지만 데스크톱에서 의도한 레이아웃과 다르고 가독성이 떨어짐.
  **아이폰 390px 세로에서는 캔버스가 화면 폭을 거의 채우므로 이 문제가 나타나지 않음을 확인**
  (`docs/screenshots/24-iphone-stage1-play.png` 참고 — 아이폰에서는 상단/하단 띠가 캔버스와 잘 맞음).

### [사소] 정지 화면 클리어 후 `friendsMet`가 `boolean[]`이어야 하는데 `false`가 `null`로 저장됨

- **어디서**: 전체 흐름 — 스테이지 1/2/3/5처럼 **그 판에 구출 가능한 친구가 없는 스테이지**를 클리어할 때마다
- **재현 절차**:
  1. 새 프로필로 스테이지 1을 클리어
  2. `localStorage`의 `zombie-school:v1` → `friendsMet` 값을 확인
- **증상**: `App.tsx`의 `handleClear`가
  `next.friendsMet = data.friendsMet.map((v, i) => v || r.friendsMet[i])`로 병합하는데,
  `ClearResult.friendsMet`(`GameCanvas.tsx`)는 `s.friends.map(f => f.state === "follow")`로 만들어진다.
  스테이지 1/2/3/5의 맵에는 구출 가능한 친구(`F`)가 아예 없어 `s.friends`가 **빈 배열**이고,
  따라서 `r.friendsMet[i]`는 4칸 전부 `undefined`다. `false || undefined`는 `undefined`가 되고,
  이게 JSON으로 저장되며 `null`로 직렬화된다. 즉 **스테이지 4 이전에 어떤 스테이지든 한 번만 깨도
  아직 못 구한 친구 칸이 전부 `false`에서 `null`로 바뀐다.** 실측: 스테이지 1 클리어 직후
  `friendsMet: [null,null,null,null]`.
- **영향**: `EndingScreen.tsx`와 `StageSelect`는 전부 `if (friendsMet[i])` / `.filter(Boolean)`처럼
  참·거짓만 따지므로 **현재는 화면에 보이는 증상이 없다** (null도 false와 동일하게 "안 만남"으로 처리됨).
  다만 타입 선언(`boolean[]`)과 실제 저장값이 어긋나 있어, 나중에 `friendsMet[i] === false`처럼
  엄격 비교를 쓰는 코드가 추가되면 조용히 깨질 수 있다.
- **증거**: 02-stage1-clear-persist.js 실행 로그 —
  `RESULT: localStorage-after-clear= {...,"friendsMet":[null,null,null,null],...}`
- **심각도**: 사소 — 지금 당장 눈에 보이는 버그는 아니고, 데이터 정합성 문제.

---

## 확인된 정상 동작

### 1. 데스크톱 플로우
- 타이틀 → "시작하기" → 이름 입력 화면 정상 진입. 콘솔 오류 없음.
- 이름 입력: 빈 이름이면 "이걸로 할래!" 버튼이 `disabled` 유지 (정상).
- 이름 입력: 정상 이름("하린") 입력 시 버튼 활성화 (정상).
- 이름 입력: `maxLength=6` 강제 — 9자("가나다라마바사아자")를 넣어도 input value가 "가나다라마바"(6자)로 잘림.
- 외모 선택(머리색/옷색 스와치) 렌더링 정상, 스테이지 선택 화면 진입 정상.
- 스테이지 선택: 스테이지 1만 해금, 2~5는 `disabled`로 잠김 표시.
- 스테이지 1 진입: 미니맵 오버레이, HUD, 구역 카드("1-1 현관 → 복도"), 무전 대사
  ("하린아, 다들 좀 이상해. 조심해!") 정상 표시.
- 재료 2개 획득 + 출구 오픈 + 출구 타일 진입 → `stageClear` 정상 동작 (엔진 로직 `exitOpen`/`finishStage` 경유 확인).
- 스테이지 클리어 화면: 별 개수, 항목별 점수(재료/급식표/클리어/시간보너스/들키지 않았어!/쌩쌩해요!), 합계 정상.
- `localStorage` 저장: `unlockedStage: 2`, `stars: [1,0,0,0,0]`, `best: [4794,0,0,0,0]` 정상 기록.
- 새로고침 후 "이어하기" → 스테이지 1 별점(★☆☆)·최고점수 유지 + **스테이지 2 잠금 해제** 확인. 진행도 영구 저장 정상.

### 2. 아이폰 13(390×844) 터치 전용 플로우
- 타이틀 → 이름 입력(추천 이름 "하린" 탭) → 머리/옷 스와치 탭 → "이걸로 할래!" 탭 → 스테이지 선택 탭 →
  스테이지 1 진입까지 **터치만으로** 완주. 콘솔 오류 0건.
- 상호작용(손) 버튼 bounding box: `left:286 right:374 top:556 bottom:644`(88×88) —
  오른쪽 끝에서 16px, 화면 아래에서 200px로 DESIGN.md §2 "오른쪽 16~180px, 아래 20~200px" 범위 안.
- 이 스테이지(1)에서는 분필/알람/손전등 버튼이 아직 안 보임 — 스테이지 1엔 해당 도구가 없으므로 정상(스펙대로).
  스테이지 3에서는 알람·손전등 버튼이 정상적으로 나타남(스크린샷 32~34).
- 하단 재료 8칸 줄 + 조작 버튼이 390px 폭 안에서 서로 겹치지 않고 잘림 없이 표시됨
  (데스크톱과 달리 모바일에서는 캔버스가 화면 폭을 거의 채워 HUD 정렬 문제가 나타나지 않음).
- 스크린샷: `docs/screenshots/20~26` (타이틀/이름입력/스테이지선택/스테이지1 플레이).

### 3. 게임오버 → 컷신 → 스킵 → 재시작
- 좀비와 충돌 시 `phase: "sleeping"` 정상 전환 (엔진 `caught()` 경유, `CATCH_DIST` 판정 확인).
- 컷신 진행 중 "딩동댕동~" → "아 깜짝이야! 얼마나 잤지?" 등 문구 정상 표시, 화면 톤 다운 확인.
- 0.4초(SKIP_AFTER_MS) 이후 탭/클릭 시 `skipCutscene()`으로 `sleepT`가 즉시 `GAMEOVER_MS`(3000)까지 점프,
  "종 치고 다시!" 버튼 등장 확인.
- "종 치고 다시!" 클릭 → `phase: "playing"` 복귀까지 **약 60ms 이내**(측정에 포함된 인위적 대기 포함치이므로
  실제로는 이보다 짧음) — DESIGN.md §6의 300ms 예산 안. BUILD_NOTES가 주장한 "17ms"와 오더가 일치.
- 재시작 후 카메라·구역(체크포인트)이 정확히 잡혀 있던 자리(예: "1-2 보건실")로 복귀 확인 —
  스테이지 처음이 아니라 **구역 시작점**으로 재개 (스펙 §6 그대로).
- 재시작 전에 주운 재료(`item.taken` + `s.gotIngredients[i]`)가 재시작 후에도 그대로 유지됨을
  `restartFromCheckpoint()`가 items/gotIngredients를 건드리지 않는 것으로 코드 레벨까지 확인.
- 재시작 전후로 배경/캐릭터 색감이 달라 보이는 현상을 발견해 별도로 재검증했으나,
  이는 렌더링 버그가 아니라 **체크포인트 지점(구역 시작점)의 카메라 위치가 잡혀 캐릭터가 처음 보던
  각도와 다른 부분을 비추는 것**뿐이었다(같은 지점을 baseline과 재대조해 색상이 100% 동일함을 확인).
- 스크린샷: `docs/screenshots/09~11`(최초 발견), `12~15`(재검증, baseline과 재시작 후 동일 화면 비교).

### 4. 스테이지 1~5 메커닉 스모크 테스트
- **스테이지 2 — B-1 아주머니 대기(dormant)**: 스테이지 진입 시 아주머니는 복도 오른쪽 끝(`x=1260,y=380`)에서
  `dormant:true`로 정지. 분필(`C`, 재료 인덱스 2)을 실제 상호작용 키(`E`)로 주운 순간 `dormant:false`로
  전환되고 1초 뒤 좌측으로 실제 이동 시작(`x:1260→1223`)까지 확인. BUILD_NOTES B-1 항목 그대로 동작.
- **스테이지 2 — 급식 아주머니 화면 밖 빨간 화살표**: 아주머니로부터 300px 떨어진 지점(화면 밖, 미니맵 반경
  320px 이내)에 플레이어를 두자 화면 오른쪽 가장자리에 흰 테두리의 빨간 삼각 화살표가 정확히 렌더링됨
  (`docs/screenshots/35-stage2-matron-arrow.png`).
- **스테이지 3 — 어두운 방 + 손전등**: 미술실(3-2) 진입 시 어두운 남보라 오버레이 + 부드러운 시야원(150px)
  확인. `F` 키로 손전등 on 시 `#FFF3D6` 계열의 부채꼴 빛이 정확히 표시되고, 최초 점등 시 무전
  "손전등 켜면 잘 보여. 근데... 더 잘 들켜." 정상 표시(스펙 문구와 일치).
  (`docs/screenshots/32-stage3-dark-room-no-torch.png`, `33-...-torch-on.png`)
- **스테이지 3 — 알람시계**: `Q`로 설치 → 배치 직후 `ringing:false` → 약 1초(`ALARM_DELAY=1000`) 뒤
  `ringing:true`로 전환 확인. 화면에는 두꺼운 주황 원이 정확히 표시됨(`34-stage3-alarm-ringing.png`).
- **스테이지 4 — 렌더 스케일 0.8 줌아웃**: `s.stage.renderScale === 0.8` 확인, 좀비 10마리/친구 4명 전부 로드됨.
- **스테이지 4 — 친구 구출/미끼 메커닉**: 연습 모드(좀비 0마리)에서 친구를 1초간 상호작용(`E`)으로 깨우자
  `state: "asleep" → "follow"`, 점수 `+700`(SCORE.friend) 정상 지급, 이후 플레이어를 움직이자 친구가
  하늘색 글로우 링을 두르고 뒤따라옴 확인(`44-...`, `45-...`).
  **부가 확인(의도된 동작)**: 실제 좀비가 있는 상태에서 같은 자리에 가만히 서서 친구를 깨우면, 깨우기
  완료 직후 근처에서 추격해 온 좀비가 22px 이내로 붙어 있어 **친구가 그 자리에서 다시 잠들고
  좀비가 2초간 하품하며 멈추는 "미끼" 메커닉이 정확히 재현됨** (`FRIEND_STALL_MS=2000` 부여 확인).
  DESIGN.md §M7 "이득 1 — 미끼"와 일치하는 정상 동작이며 버그 아님.
- **스테이지 5 — 보스 사이클**: `walk(~5.5s) → windup(~1.0s) → speech(radius 0→200, ~0.8s에 걸쳐 확장,
  총 ~3.5s 유지) → recover(~2.2s) → walk...` 순으로 정확히 순환하는 것을 0.5초 간격 샘플링으로 확인
  (총 사이클 ≈ 12.2초, DESIGN.md 부록 A와 일치). 회복 단계에서 "어험, 어험!" 대사 표시 확인
  (`docs/screenshots/50-stage5-boss-near.png`).
- **스테이지 5 — 방송 게이지**: 해독제 없이(`exitOpen:false`) 방송실 문에서 상호작용해도 게이지가
  0에서 전혀 오르지 않음을 확인(잠금 정상 동작). `exitOpen:true`로 전환 후 좀비 0마리 연습 모드에서
  누르고 있는 동안 0→500→999→3000까지 정확히 누적되고 3000 도달 시 `phase:"stageClear"`로 전환
  확인(`54-broadcast-clean-progress.png`, `55-broadcast-clean-cleared.png`). 화면 하단에 "방송 준비 n%"
  바가 항상 표시됨(스펙 §9-0 확인).
- **엔딩 화면**: 스테이지 5 클리어 후 "다음 스테이지" 클릭 → `EndingScreen` 진입, 구출한 친구(3/4)만
  플레이어 옆에 하늘색 글로우로 등장하고 못 구한 친구 자리는 비어 있음을 확인(`60-ending-screen.png`).
  `localStorage.ending: true`로 저장되어 연습 모드 해금 조건 충족 확인. 별 15개 달성 시
  `unlockedHats`에 `ribbon/crown/pudding/lunch` 4종 전부 자동 부여됨도 확인.
- 지금까지(스텝 1~4) 콘솔 에러/페이지 에러 **0건**.

---

(계속 진행 중 — 5~7단계는 아래에 이어서 기록)

### 5. 뮤트 상태 시각 신호 (§9-0 검증)
- `bgmOn:false, sfxOn:false`로 **최초 로딩부터** 음소거한 프로필로 시작 → 타이틀의 첫 사용자 제스처
  ("이어하기" 클릭)까지 포함해 스테이지 3(3-1/3-3)·5(5-1/5-2/5-3) 전 구간에서 **콘솔 에러 0건**.
  `AudioContext`가 suspended 상태이거나 `resume()`이 아예 안 불려도 `playSfx`/음악 재생 경로가
  예외를 던지지 않음(오디오 코드가 전부 `.catch(() => {})`로 감싸져 있음, `audio.ts` 코드 검토로 확인).
- HUD 우상단 빠른 음소거 아이콘이 스피커 X 모양으로 정확히 반영됨(스크린샷 72, 73에서 확인).
- **3-3 도서관**: 소리 없이도 미니맵 보라 점, 좀비 눈 발광(반경 26px 보라 발광), 빨간 `!` 경고
  (2배 크기 + 화면 진동)가 전부 시각적으로만 확인 가능 — 실제로 사운드 꺼진 상태로 스크린샷 촬영해
  확인함(`70-stage3-3-library-muted.png`, `71-stage3-3-notice-alert-muted.png`).
- **5-3 보스**: `speech` 단계에서 노란 반투명 원(반경 200) + 하단 대사 자막이, `recover` 단계에서
  초록 링으로 색이 바뀌는 것이 소리 없이도 확인됨(`72-stage5-3-boss-speech-muted.png`,
  `73-stage5-3-boss-recover-muted.png`).
- 결론: §9-0 "뮤트 상태로도 전부 클리어 가능"의 핵심 조건(정보성 소리 = 시각적 쌍 존재, 오디오
  suspended여도 예외 없음)을 코드/스크린샷 양쪽으로 확인. **실제 사람이 뮤트 상태로 3-3·5-3을
  처음부터 끝까지 플레이해 클리어하는 전 과정까지는 시간 관계상 자동화하지 못했다** — 위 확인은
  "필요한 시각 신호가 전부 뜨고 오류가 없다"는 선에서 그쳤다. 실제 클리어 가능 여부의 최종 확인은
  사람 플레이테스트로 한 번 더 보는 걸 권장.

### 6. 콘솔 오류 / 벽 끼임 / 사물함 끼임 / 좀비 벽 통과 / 프레임
- **콘솔/페이지 오류**: 이번 세션에서 실행한 모든 스크립트(데스크톱·아이폰·가로모드·뮤트·전 스테이지)를
  통틀어 **0건**.
- **벽 끼임**: 스테이지 4(가장 붐비는 스테이지, 좀비 10 + 아주머니 2)에서 WASD를 300~400ms 간격으로
  8방향 랜덤에 가깝게 16회 전환하며 6.4초간 강제 이동 → 16회 샘플링 전부 `playerInSolid:false`
  (플레이어가 벽 타일 안에 들어간 적 없음).
- **좀비 벽 통과(엔티티가 벽 타일 안에 끼는 것)**: 같은 스트레스 테스트에서 스테이지4의 좀비 10마리
  전원을 매 샘플(16회)마다 검사 — `zombiesInSolidTile:0`으로 전부 통과(좀비가 solid 타일 위에
  머무른 적 없음).
- **사물함 끼임**: 스테이지1 사물함에 진입(`hiding:183`) → 즉시 퇴장(`hiding:-1`) → 쿨다운 중 재진입
  시도(`LOCKER_CD=1.2s`)는 조용히 무시되고 `hiding:-1`로 안전하게 유지됨. 갇히거나 무한 대기 없음.
- **프레임**: 헤드리스 Chromium에서 스테이지4(줌아웃 0.8, 엔티티 최다) 이동 중 `requestAnimationFrame`
  카운트로 측정한 순수 페이지 프레임레이트 ≈ **60fps** (389프레임/6.48초). 이는 실제 저사양 모바일
  기기의 체감 성능을 보장하지 않지만, 메인 스레드 블로킹이나 GC 스톨로 인한 눈에 띄는 프레임 드랍은
  이 환경에서 관찰되지 않았다. **실기기(특히 구형 아이폰) FPS 실측은 이 자동화로 확인 불가 — 별도 확인 필요.**

### 7. 폰 가로 모드
- 844×390(가로) + `hasTouch/isMobile`로 접속 후 스테이지 1 진입 → "폰을 세워 주세요" +
  "이 게임은 세로로 하는 게 제일 재밌어!" 안내 오버레이가 즉시 표시됨(`91-landscape-844x390-rotate-notice.png`).
  이때 엔진 상태 `s.paused === true`로 실제 게임 로직(좀비 이동 등)도 정지됨을 확인(코드상 `update()`가
  `s.paused`에서 조기 반환 — 눕혀 놓고 방치해도 몰래 진행되지 않음).
  HUD 입력(터치 조작 등)도 이 상태에서 비활성화됨(`releaseInteract`, `setMove(s,0,0)` 강제 호출 확인).
- 390×844(세로)로 다시 뷰포트를 돌리자 안내가 즉시 사라지고 `s.paused === false`로 정상 복귀,
  캔버스·HUD·터치 버튼이 다시 정상 배치됨(`92-back-to-portrait.png`). 재조작 가능.

---

## 최종 버그 목록 (요약)

1. **[보통]** 데스크톱 와이드 화면(1280×800 등)에서 상단 HUD 띠(점수/급식표/일시정지)와 하단 재료 8칸 줄이
   `w-full`로 뷰포트 전체 폭에 늘어나, 화면 가운데 좁게 렌더링되는 게임 캔버스와 정렬이 안 맞고 화면
   양쪽 가장자리에 붙는다. 아이폰 세로 390px, 가로 844px 등 좁은 뷰포트에서는 캔버스가 폭을 거의
   채우므로 증상이 나타나지 않는다 — **데스크톱/넓은 태블릿에서만 재현**.
2. **[사소]** `App.tsx`의 `handleClear` 병합 로직 때문에, 그 판에 구출 가능한 친구가 없는 스테이지
   (1·2·3·5)를 한 번이라도 클리어하면 아직 못 구한 친구 슬롯이 `friendsMet: false`에서
   `friendsMet: null`로 조용히 바뀐다(타입은 `boolean[]`인데 실제로는 `null`이 섞임). 모든 소비처가
   진위(truthy) 판정만 쓰기 때문에 **현재는 화면에 드러나는 증상 없음** — 데이터 정합성 문제로만 기록.

## 확인하지 못한 것 (명시)
- 뮤트 상태로 **사람이 실제로 3-3/5-3을 처음부터 끝까지 클리어**하는 전체 플레이 — 시각 신호와
  무오류만 자동으로 검증했고, "정말 소리 없이 이겨지는가"의 최종 체감은 사람 테스트 권장.
- 저사양/실기기(특히 구형 아이폰)에서의 실측 FPS — 헤드리스 Chromium 60fps는 참고치일 뿐 보장 아님.
- 스테이지 3 실측 클리어 시간(par 130초 대비) — 이번 세션은 디버그 훅으로 메커니즘 단위 검증에
  집중해 처음부터 끝까지 손으로 클리어하며 시간을 재는 것은 하지 못했다.
- `nearMiss`(아슬아슬) 연출이 추격 중 실제로 놀라게 하는지의 주관적 판단 — audience 영역에 가까워
  이번 버그 중심 테스트에서는 다루지 않음.

## 통과 요약
- 데스크톱 1280×800 키보드: 타이틀→이름(정상/빈칸/6자 제한)→외모→스테이지선택→스테이지1 클리어→
  새로고침 저장 유지→스테이지2 해금 **전부 통과**.
- 아이폰 13(390×844) 터치 전용: 같은 플로우 터치만으로 완주, 버튼 4개 전부 엄지 반경 안, 겹침 없음
  **통과**.
- 게임오버→잠들기 컷신→0.4초 후 스킵→재시작(<300ms 예산 안)→체크포인트로 복귀→기존 재료 유지
  **전부 통과**.
- 스테이지 1~5 핵심 메커니즘(사물함/분필/알람/문잠금/어둠+손전등/친구구출+미끼/아주머니 대기·화살표/
  보스 4단계 사이클/방송 게이지 잠금·누적/엔딩+모자 해금) **전부 확인, 스펙과 수치 일치**.
- 뮤트 상태에서 스테이지 3·5 핵심 시각 신호 전부 표시 + 콘솔 오류 0건 **통과** (전체 사람 플레이
  클리어까지는 미검증, 위 참고).
- 벽 끼임 0 / 좀비 벽 통과 0 / 사물함 끼임 없음 / 콘솔 오류 0(전 세션) / 헤드리스 기준 60fps **통과**.
- 가로 모드 회전 안내 + 일시정지, 세로 복귀 시 정상 재개 **통과**.
- 발견된 버그는 **보통 1건**(데스크톱 HUD 정렬), **사소 1건**(friendsMet null 오염) 뿐이며 둘 다
  플레이를 막지 않는다.
